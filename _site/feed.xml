<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="http://localhost:4000/feed.xml" rel="self" type="application/atom+xml" /><link href="http://localhost:4000/" rel="alternate" type="text/html" /><updated>2026-07-28T13:42:37+02:00</updated><id>http://localhost:4000/feed.xml</id><title type="html">K8sReady</title><subtitle>Platform Engineering, Kubernetes and Open Source Infrastructure Tooling</subtitle><entry xml:lang="es"><title type="html">cmdpeek te permite descubrir workflows de terminal sin memorizar aliases</title><link href="http://localhost:4000/2026/07/27/cmd-peek-es/" rel="alternate" type="text/html" title="cmdpeek te permite descubrir workflows de terminal sin memorizar aliases" /><published>2026-07-27T00:00:00+02:00</published><updated>2026-07-27T00:00:00+02:00</updated><id>http://localhost:4000/2026/07/27/cmd-peek-es</id><content type="html" xml:base="http://localhost:4000/2026/07/27/cmd-peek-es/"><![CDATA[<p>Los aliases de shell son útiles cuando los recuerdas.</p>

<p>El problema aparece cuando tu colección crece tanto que ya no recuerdas:</p>

<ul>
  <li>el nombre exacto del alias</li>
  <li>qué aliases existen</li>
  <li>cuál resuelve un problema concreto</li>
  <li>qué argumentos espera un comando</li>
  <li>si un comando es seguro para ejecutarlo tal cual</li>
</ul>

<p>En ese momento, los aliases dejan de ser una ayuda de productividad y empiezan a convertirse en otra cosa que tienes que memorizar.</p>

<p>Ese es el problema que quería resolver con <strong>cmdpeek</strong>.</p>

<p><code class="language-plaintext highlighter-rouge">cmdpeek</code> es una paleta de comandos open source para descubrir y ejecutar workflows reutilizables de terminal.</p>

<p>En lugar de recordar un alias exacto como:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>klo
</code></pre></div></div>

<p>puedes buscar por lo que quieres hacer:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>cmdpeek kubernetes logs
</code></pre></div></div>

<p>El comando no tiene que coincidir exactamente con el nombre interno.</p>

<p><code class="language-plaintext highlighter-rouge">cmdpeek</code> busca en:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>title
name
description
labels
command contents
</code></pre></div></div>

<p>Esto significa que un workflow puede encontrarse usando palabras distintas, nombres antiguos de aliases, conceptos técnicos o incluso una parte del comando que ejecuta por debajo.</p>

<blockquote>
  <p><strong>Los aliases optimizan comandos que recuerdas. cmdpeek te ayuda a descubrir los que no recuerdas.</strong></p>
</blockquote>

<hr />

<h2 id="por-qué-los-aliases-dejan-de-escalar">Por qué los aliases dejan de escalar</h2>

<p>Una colección pequeña de aliases es fácil de gestionar:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">alias </span><span class="nv">k</span><span class="o">=</span><span class="s1">'kubectl'</span>
<span class="nb">alias </span><span class="nv">kgp</span><span class="o">=</span><span class="s1">'kubectl get pods'</span>
<span class="nb">alias </span><span class="nv">klo</span><span class="o">=</span><span class="s1">'kubectl logs -f'</span>
</code></pre></div></div>

<p>Pero con el tiempo, estas colecciones suelen crecer hasta tener decenas o cientos de entradas relacionadas con:</p>

<ul>
  <li>Kubernetes</li>
  <li>Flux</li>
  <li>Git</li>
  <li>GitHub Actions</li>
  <li>AWS</li>
  <li>Terraform</li>
  <li>desarrollo local</li>
  <li>troubleshooting</li>
  <li>conversión multimedia</li>
  <li>despliegues</li>
</ul>

<p>Los comandos pueden seguir siendo útiles, pero recordar sus nombres se vuelve difícil.</p>

<p>Puede que recuerdes que creaste algo para:</p>

<ul>
  <li>descargar logs de GitHub Actions</li>
  <li>forzar la reconciliación de un HelmRelease de Flux</li>
  <li>cambiar el contexto de Kubernetes</li>
  <li>inspeccionar shards de Elasticsearch</li>
  <li>convertir un GIF en MP4</li>
</ul>

<p>Pero puede que no recuerdes si el alias se llamaba:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ghlogs
download-run
gha-debug
flux-force
hr-reconcile
gif2mp4
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">cmdpeek</code> elimina esa dependencia de nombres exactos.</p>

<p>Un comando puede incluir metadatos ricos y buscables:</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">force-reconcile-flux-helmrelease</span>
  <span class="na">title</span><span class="pi">:</span> <span class="s">Force Flux HelmRelease reconciliation</span>
  <span class="na">description</span><span class="pi">:</span> <span class="s">Select a namespace and HelmRelease, then force Flux to reconcile it with its source.</span>
  <span class="na">labels</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="s">flux</span>
    <span class="pi">-</span> <span class="s">fluxcd</span>
    <span class="pi">-</span> <span class="s">helm</span>
    <span class="pi">-</span> <span class="s">helmrelease</span>
    <span class="pi">-</span> <span class="s">reconcile</span>
    <span class="pi">-</span> <span class="s">force</span>
    <span class="pi">-</span> <span class="s">kubernetes</span>
    <span class="pi">-</span> <span class="s">gitops</span>
    <span class="pi">-</span> <span class="s">troubleshooting</span>
</code></pre></div></div>

<p>Ese mismo comando puede encontrarse con búsquedas como:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>flux force
helm reconcile
gitops troubleshooting
helmrelease
</code></pre></div></div>

<p>Los nombres históricos de aliases también pueden mantenerse como labels, de modo que un atajo mental antiguo siga funcionando sin necesidad de definir otro alias de shell.</p>

<hr />

<h2 id="buscar-por-intención">Buscar por intención</h2>

<p>La idea principal de <code class="language-plaintext highlighter-rouge">cmdpeek</code> es sencilla:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Buscar por intención
        ↓
Seleccionar un workflow
        ↓
Resolver sus variables
        ↓
Revisar el comando generado
        ↓
Confirmar y ejecutar
</code></pre></div></div>

<p>El catálogo se define en YAML, lo que facilita revisar, versionar, compartir y extender los workflows.</p>

<p>Un comando puede incluir:</p>

<ul>
  <li>un nombre interno estable</li>
  <li>un título legible</li>
  <li>una descripción detallada</li>
  <li>labels buscables</li>
  <li>un script de shell</li>
  <li>variables interactivas</li>
  <li>selectores dinámicos generados por comandos</li>
</ul>

<p>Por ejemplo:</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">pod-logs</span>
  <span class="na">title</span><span class="pi">:</span> <span class="s">Follow Kubernetes pod logs</span>
  <span class="na">description</span><span class="pi">:</span> <span class="s">Select a namespace and stream logs from one of its pods</span>
  <span class="na">labels</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="s">kubernetes</span>
    <span class="pi">-</span> <span class="s">pods</span>
    <span class="pi">-</span> <span class="s">logs</span>
    <span class="pi">-</span> <span class="s">troubleshooting</span>
    <span class="pi">-</span> <span class="s">stream</span>
    <span class="pi">-</span> <span class="s">klo</span>
  <span class="na">run</span><span class="pi">:</span> <span class="s">kubectl logs -f "" -n ""</span>
</code></pre></div></div>

<p>Este workflow puede encontrarse mediante:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>pod logs
kubernetes troubleshooting
stream
klo
</code></pre></div></div>

<p>El nombre exacto deja de ser el único punto de entrada.</p>

<hr />

<h2 id="demo-buscar-dentro-de-un-catálogo-grande">Demo: buscar dentro de un catálogo grande</h2>

<p><img width="2800" height="1800" alt="cmdpeek-search-scroll-demo" src="https://github.com/user-attachments/assets/afc4dbc5-904a-4005-b221-21bff9f7fae3" /></p>

<p>El catálogo interactivo soporta:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>/         Buscar
↑ / ↓     Navegar
← / →     Cambiar de página
e         Abrir detalles
Enter     Seleccionar
q         Salir
</code></pre></div></div>

<p>Las búsquedas se ordenan según la relevancia de los metadatos del comando.</p>

<p>También se pueden combinar varios términos:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>github logs
production deploy
elasticsearch size
kubernetes context
</code></pre></div></div>

<p>Un resultado puede coincidir por su título, descripción, labels, nombre interno o contenido del script.</p>

<p>Esto es especialmente útil para comandos importantes que no usas con suficiente frecuencia como para recordarlos.</p>

<hr />

<h2 id="empezar-una-búsqueda-directamente-desde-la-línea-de-comandos">Empezar una búsqueda directamente desde la línea de comandos</h2>

<p>También puedes pasar términos de búsqueda directamente después de <code class="language-plaintext highlighter-rouge">cmdpeek</code>:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>cmdpeek kubernetes context
cmdpeek github logs
cmdpeek elasticsearch shards
</code></pre></div></div>

<p>Los argumentos se unen y se convierten en una búsqueda inicial.</p>

<p>El comportamiento depende del número de resultados:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>nombre exacto del comando → abre ese workflow directamente
un único resultado        → abre ese workflow directamente
varios resultados         → muestra el catálogo ya filtrado
ningún resultado          → muestra el catálogo sin coincidencias
</code></pre></div></div>

<p>Esto permite que <code class="language-plaintext highlighter-rouge">cmdpeek</code> funcione como un sistema de aliases descubrible.</p>

<p>Cuando sabes aproximadamente lo que quieres, puedes empezar con:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>cmdpeek elastic
</code></pre></div></div>

<p>en lugar de abrir el catálogo completo y escribir el filtro manualmente.</p>

<hr />

<h2 id="demo-búsqueda-directa-y-selección-automática">Demo: búsqueda directa y selección automática</h2>

<p><img width="1800" height="800" alt="cmdpeek-search-demo-filter" src="https://github.com/user-attachments/assets/ec0f0579-82d6-4d92-9e98-63ddb3161705" /></p>

<p>Cuando una búsqueda devuelve varios workflows, <code class="language-plaintext highlighter-rouge">cmdpeek</code> abre la lista filtrada.</p>

<p>Cuando la búsqueda devuelve solo uno, se salta la lista y entra directamente en el comando.</p>

<p>Por ejemplo:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>cmdpeek gif
</code></pre></div></div>

<p>puede abrir directamente un workflow de conversión de GIF a MP4 si es el único resultado.</p>

<p>Esto mantiene la herramienta rápida cuando ya conoces la intención, pero conserva la capacidad de descubrimiento cuando la búsqueda es más amplia.</p>

<hr />

<h2 id="variables-interactivas-en-lugar-de-aliases-frágiles">Variables interactivas en lugar de aliases frágiles</h2>

<p>Los aliases se vuelven difíciles de mantener cuando los comandos necesitan parámetros.</p>

<p>Un alias puede funcionar bien para un comando fijo:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">alias </span><span class="nv">klo</span><span class="o">=</span><span class="s1">'kubectl logs -f'</span>
</code></pre></div></div>

<p>Pero los workflows reales suelen necesitar valores como:</p>

<ul>
  <li>namespace</li>
  <li>pod</li>
  <li>entorno</li>
  <li>branch</li>
  <li>ruta de fichero</li>
  <li>HelmRelease</li>
  <li>URL de GitHub Actions</li>
  <li>mensaje de commit</li>
</ul>

<p><code class="language-plaintext highlighter-rouge">cmdpeek</code> soporta cuatro fuentes de variables:</p>

<table>
  <thead>
    <tr>
      <th>Fuente</th>
      <th>Propósito</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">input</code></td>
      <td>Entrada libre del usuario</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">options</code></td>
      <td>Valores estáticos seleccionables</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">environment</code></td>
      <td>Valor inicial cargado desde una variable de entorno</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">command</code></td>
      <td>Opciones generadas ejecutando un comando</td>
    </tr>
  </tbody>
</table>

<p>Un workflow de Kubernetes puede generar namespaces dinámicamente:</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">variables</span><span class="pi">:</span>
  <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">namespace</span>
    <span class="na">prompt</span><span class="pi">:</span> <span class="s">Select Kubernetes namespace</span>
    <span class="na">source</span><span class="pi">:</span>
      <span class="na">type</span><span class="pi">:</span> <span class="s">command</span>
      <span class="na">command</span><span class="pi">:</span> <span class="pi">&gt;-</span>
        <span class="s">kubectl get namespaces</span>
        <span class="s">-o jsonpath='{range .items[*]}{.metadata.name}{"\n"}{end}'</span>
</code></pre></div></div>

<p>Una segunda variable puede depender de la primera:</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code>  <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">pod</span>
    <span class="na">prompt</span><span class="pi">:</span> <span class="s">Select pod</span>
    <span class="na">source</span><span class="pi">:</span>
      <span class="na">type</span><span class="pi">:</span> <span class="s">command</span>
      <span class="na">command</span><span class="pi">:</span> <span class="pi">&gt;-</span>
        <span class="s">kubectl get pods -n ""</span>
        <span class="s">-o jsonpath='{range .items[*]}{.metadata.name}{"\n"}{end}'</span>
</code></pre></div></div>

<p>Esto convierte un comando en un workflow guiado, en lugar de una cadena que tienes que recordar y editar manualmente.</p>

<hr />

<h2 id="selectores-dinámicos">Selectores dinámicos</h2>

<p>Las variables generadas mediante comandos son especialmente útiles para workflows de DevOps.</p>

<p>Pueden listar:</p>

<ul>
  <li>namespaces de Kubernetes</li>
  <li>pods</li>
  <li>contextos</li>
  <li>HelmReleases de Flux</li>
  <li>branches de Git</li>
  <li>perfiles de AWS</li>
  <li>ficheros</li>
  <li>releases</li>
  <li>entornos</li>
</ul>

<p>Por ejemplo, un workflow de reconciliación de Flux puede listar primero los namespaces y después mostrar únicamente los HelmReleases del namespace seleccionado:</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">variables</span><span class="pi">:</span>
  <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">namespace</span>
    <span class="na">prompt</span><span class="pi">:</span> <span class="s">Select Kubernetes namespace</span>
    <span class="na">source</span><span class="pi">:</span>
      <span class="na">type</span><span class="pi">:</span> <span class="s">command</span>
      <span class="na">command</span><span class="pi">:</span> <span class="s">kubectl get namespaces -o name</span>

  <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">helmrelease</span>
    <span class="na">prompt</span><span class="pi">:</span> <span class="s">Select Flux HelmRelease</span>
    <span class="na">source</span><span class="pi">:</span>
      <span class="na">type</span><span class="pi">:</span> <span class="s">command</span>
      <span class="na">command</span><span class="pi">:</span> <span class="pi">&gt;-</span>
        <span class="s">flux get helmreleases</span>
        <span class="s">--namespace ""</span>
        <span class="s">--no-header</span>
        <span class="s">| awk '{print $1}'</span>
</code></pre></div></div>

<p>El usuario no tiene que recordar ni escribir manualmente los nombres de los recursos.</p>

<hr />

<h2 id="preview-antes-de-ejecutar">Preview antes de ejecutar</h2>

<p>Los valores de las variables se insertan en la previsualización a medida que se seleccionan.</p>

<p>Los comandos y scripts largos siguen siendo desplazables, lo que permite revisar el resultado final antes de ejecutarlo.</p>

<p>Cuando todas las variables están resueltas, <code class="language-plaintext highlighter-rouge">cmdpeek</code> muestra el comando completamente generado y exige una confirmación explícita:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Execute this command?

╭────────────────────────────────────────────╮
│ flux reconcile helmrelease svc-time       │
│   -n devbox-phoenix-2                     │
│   --with-source                           │
│   --force                                 │
╰────────────────────────────────────────────╯

y execute   n cancel   enter cancel
</code></pre></div></div>

<p>Esto es especialmente útil para comandos relacionados con:</p>

<ul>
  <li>entornos de producción</li>
  <li>despliegues</li>
  <li>eliminaciones</li>
  <li>reconciliaciones forzadas</li>
  <li>pushes</li>
  <li>migraciones</li>
  <li>cambios de infraestructura</li>
</ul>

<p>El comando permanece visible antes de ejecutarse, en lugar de quedar oculto detrás de un alias.</p>

<hr />

<h2 id="modo-no-interactivo">Modo no interactivo</h2>

<p>El descubrimiento interactivo es útil cuando no recuerdas el workflow exacto.</p>

<p>Pero a veces ya conoces el nombre del comando y quieres reutilizarlo desde:</p>

<ul>
  <li>una función de shell</li>
  <li>un script</li>
  <li>CI</li>
  <li>documentación</li>
  <li>otra herramienta</li>
</ul>

<p>Para esos casos, <code class="language-plaintext highlighter-rouge">cmdpeek</code> también soporta ejecución no interactiva:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>cmdpeek <span class="se">\</span>
  <span class="nt">--no-interactive</span> <span class="se">\</span>
  <span class="nt">--name</span> gcommit <span class="se">\</span>
  <span class="nt">--set</span> <span class="s2">"message=Add non-interactive execution demo"</span>
</code></pre></div></div>

<p>Los valores pueden proporcionarse mediante flags <code class="language-plaintext highlighter-rouge">--set</code> repetibles:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>cmdpeek <span class="se">\</span>
  <span class="nt">--no-interactive</span> <span class="se">\</span>
  <span class="nt">--name</span> deploy <span class="se">\</span>
  <span class="nt">--set</span> <span class="nv">environment</span><span class="o">=</span>staging <span class="se">\</span>
  <span class="nt">--set</span> <span class="nv">version</span><span class="o">=</span>1.4.2
</code></pre></div></div>

<p>El mismo catálogo YAML puede soportar tanto:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>descubrimiento interactivo
como
ejecución automatizable
</code></pre></div></div>

<hr />

<h2 id="demo-reutilizar-un-workflow-de-forma-no-interactiva">Demo: reutilizar un workflow de forma no interactiva</h2>

<p><img width="1800" height="800" alt="cmdpeek-non-interactive-gcommit" src="https://github.com/user-attachments/assets/bf6ea668-159a-4379-b0ed-fe233c17c92e" /></p>

<p>Este modo sigue mostrando el comando y pidiendo confirmación, salvo que se use <code class="language-plaintext highlighter-rouge">--yes</code>.</p>

<p>También existe un modo dry-run:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>cmdpeek <span class="se">\</span>
  <span class="nt">--no-interactive</span> <span class="se">\</span>
  <span class="nt">--name</span> gcommit <span class="se">\</span>
  <span class="nt">--set</span> <span class="s2">"message=Preview this commit"</span> <span class="se">\</span>
  <span class="nt">--dry-run</span>
</code></pre></div></div>

<p>Esto muestra el comando final sin ejecutarlo.</p>

<hr />

<h2 id="catálogos-locales-y-compartidos">Catálogos locales y compartidos</h2>

<p>Por defecto, <code class="language-plaintext highlighter-rouge">cmdpeek</code> busca:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>.cmdpeek.yaml
</code></pre></div></div>

<p>También se puede configurar una ruta local mediante:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">export </span><span class="nv">CMDPEEK_CONFIG_FILE</span><span class="o">=</span><span class="s2">"</span><span class="nv">$HOME</span><span class="s2">/.config/cmdpeek/commands.yaml"</span>
</code></pre></div></div>

<p>Los catálogos también pueden cargarse desde GitHub:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">export </span><span class="nv">CMDPEEK_CONFIG_GITHUB</span><span class="o">=</span><span class="s2">"company/platform-config:cmdpeek/commands.yaml@main"</span>
</code></pre></div></div>

<p>Para repositorios privados, la autenticación puede proporcionarse mediante:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">export </span><span class="nv">CMDPEEK_GITHUB_TOKEN</span><span class="o">=</span><span class="s2">"github_pat_..."</span>
</code></pre></div></div>

<p>Las configuraciones remotas se almacenan en caché y se actualizan usando ETags de GitHub.</p>

<p>Esto permite mantener:</p>

<ul>
  <li>un catálogo personal</li>
  <li>un catálogo específico por repositorio</li>
  <li>un catálogo compartido por equipo</li>
  <li>un catálogo versionado para platform engineering</li>
</ul>

<p>El fichero YAML se convierte en documentación ejecutable para workflows que, de otro modo, estarían repartidos entre aliases, historial de shell, snippets y README.</p>

<hr />

<h2 id="ejemplos-de-workflows">Ejemplos de workflows</h2>

<p>El mismo catálogo puede describir tareas muy distintas.</p>

<h3 id="analizar-logs-de-github-actions">Analizar logs de GitHub Actions</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>cmdpeek github logs
</code></pre></div></div>

<p>El workflow puede pedir una URL de un run o job de GitHub Actions, descargar los logs, extraerlos y prepararlos para analizarlos.</p>

<h3 id="forzar-la-reconciliación-de-un-helmrelease-de-flux">Forzar la reconciliación de un HelmRelease de Flux</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>cmdpeek flux force
</code></pre></div></div>

<p>El workflow puede seleccionar interactivamente un namespace y un HelmRelease antes de ejecutar:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>flux reconcile helmrelease &lt;name&gt; <span class="se">\</span>
  <span class="nt">--namespace</span> &lt;namespace&gt; <span class="se">\</span>
  <span class="nt">--with-source</span> <span class="se">\</span>
  <span class="nt">--force</span>
</code></pre></div></div>

<h3 id="cambiar-el-contexto-de-kubernetes">Cambiar el contexto de Kubernetes</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>cmdpeek kubernetes context
</code></pre></div></div>

<p>Los contextos disponibles pueden generarse dinámicamente desde el kubeconfig actual.</p>

<h3 id="convertir-un-gif-en-mp4">Convertir un GIF en MP4</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>cmdpeek gif mp4
</code></pre></div></div>

<p>El workflow puede pedir las rutas de entrada y salida y ejecutar siempre la misma conversión con <code class="language-plaintext highlighter-rouge">ffmpeg</code>.</p>

<h3 id="ejecutar-un-workflow-de-git-commit">Ejecutar un workflow de Git commit</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>cmdpeek <span class="se">\</span>
  <span class="nt">--no-interactive</span> <span class="se">\</span>
  <span class="nt">--name</span> gcommit <span class="se">\</span>
  <span class="nt">--set</span> <span class="s2">"message=Document cmdpeek workflows"</span>
</code></pre></div></div>

<hr />

<h2 id="instalación">Instalación</h2>

<h3 id="homebrew">Homebrew</h3>

<p>Linux y macOS:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>brew tap pierinho13/tools
brew <span class="nb">install</span> <span class="nt">--cask</span> cmdpeek
</code></pre></div></div>

<p>Actualizar:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>brew update
brew upgrade <span class="nt">--cask</span> cmdpeek
</code></pre></div></div>

<hr />

<h3 id="github-releases">GitHub Releases</h3>

<p>Hay binarios precompilados disponibles a través de GitHub Releases.</p>

<hr />

<h3 id="compilar-desde-el-código-fuente">Compilar desde el código fuente</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git clone https://github.com/pierinho13/cmdpeek.git
<span class="nb">cd </span>cmdpeek

go build <span class="nt">-o</span> cmdpeek ./cmd/cmdpeek
<span class="nb">sudo mv </span>cmdpeek /usr/local/bin/
</code></pre></div></div>

<hr />

<h2 id="inicio-rápido">Inicio rápido</h2>

<p>Crea <code class="language-plaintext highlighter-rouge">.cmdpeek.yaml</code>:</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">version</span><span class="pi">:</span> <span class="m">1</span>
<span class="na">shell</span><span class="pi">:</span> <span class="s">bash</span>

<span class="na">commands</span><span class="pi">:</span>
  <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">greet</span>
    <span class="na">title</span><span class="pi">:</span> <span class="s">Greet a person</span>
    <span class="na">description</span><span class="pi">:</span> <span class="s">Print a personalized greeting</span>
    <span class="na">labels</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s">example</span>
      <span class="pi">-</span> <span class="s">greeting</span>
      <span class="pi">-</span> <span class="s">hello</span>
    <span class="na">run</span><span class="pi">:</span> <span class="pi">|</span>
      <span class="s">name=""</span>
      <span class="s">echo "Hello, ${name}!"</span>
    <span class="na">variables</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">name</span>
        <span class="na">prompt</span><span class="pi">:</span> <span class="s">Person name</span>
        <span class="na">default</span><span class="pi">:</span> <span class="s">world</span>
        <span class="na">source</span><span class="pi">:</span>
          <span class="na">type</span><span class="pi">:</span> <span class="s">input</span>
</code></pre></div></div>

<p>Después ejecuta:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>cmdpeek
</code></pre></div></div>

<p>O busca directamente:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>cmdpeek greeting
</code></pre></div></div>

<hr />

<h2 id="por-qué-no-usar-simplemente-aliases-fzf-o-un-task-runner">¿Por qué no usar simplemente aliases, fzf o un task runner?</h2>

<p><code class="language-plaintext highlighter-rouge">cmdpeek</code> no pretende reemplazar todos los aliases, fuzzy finders o task runners.</p>

<p>Los aliases siguen siendo ideales para comandos muy cortos que usas constantemente y recuerdas con facilidad.</p>

<p>Los task runners son excelentes para tareas de build y automatización definidas dentro de un repositorio.</p>

<p><code class="language-plaintext highlighter-rouge">fzf</code> es excelente para filtrar cualquier flujo de texto.</p>

<p>El enfoque de <code class="language-plaintext highlighter-rouge">cmdpeek</code> es diferente:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>descubrimiento estructurado por intención
+
resolución guiada de variables
+
preview en tiempo real
+
ejecución segura
</code></pre></div></div>

<p>Los metadatos buscables forman parte del propio modelo del comando.</p>

<p>Un workflow no es solo una cadena de shell. Tiene título, descripción, labels, variables y comportamiento de ejecución.</p>

<p>Eso hace que sea más fácil descubrirlo meses después, compartirlo con otras personas y evolucionarlo sin crear más aliases.</p>

<hr />

<h2 id="reflexiones-finales">Reflexiones finales</h2>

<p>El objetivo principal de <code class="language-plaintext highlighter-rouge">cmdpeek</code> no es simplemente ejecutar comandos.</p>

<p>El objetivo es hacer descubribles los workflows útiles de terminal.</p>

<p>Un comando que no recuerdas prácticamente no existe cuando lo necesitas.</p>

<p>Al buscar nombres, títulos, descripciones, labels y contenido del propio comando, <code class="language-plaintext highlighter-rouge">cmdpeek</code> permite encontrar workflows usando las palabras que recuerdas en ese momento.</p>

<p>Eso es lo que lo diferencia de un fichero enorme de aliases.</p>

<p>En lugar de preguntarte:</p>

<blockquote>
  <p>¿Cómo llamé a ese alias?</p>
</blockquote>

<p>puedes preguntarte:</p>

<blockquote>
  <p>¿Qué quiero hacer?</p>
</blockquote>

<p>El proyecto es open source:</p>

<p><strong>https://github.com/pierinho13/cmdpeek</strong></p>

<p>Las contribuciones, feedback, nuevos ejemplos de workflows, mejoras de documentación, tests, soporte de packaging e ideas para la TUI son muy bienvenidas.</p>

<p>Si te interesa este tipo de trabajo de platform engineering, puedes conocer mejor mi experiencia o <a href="/es/contact">contactar conmigo</a>.</p>]]></content><author><name></name></author><summary type="html"><![CDATA[cmdpeek es una paleta de comandos buscable para workflows reutilizables de terminal. Te permite encontrar comandos por intención usando nombres, títulos, descripciones, labels y el contenido del propio comando, resolver variables, revisar el resultado final y ejecutarlo de forma segura.]]></summary><media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="https://github.com/user-attachments/assets/afc4dbc5-904a-4005-b221-21bff9f7fae3" /><media:content medium="image" url="https://github.com/user-attachments/assets/afc4dbc5-904a-4005-b221-21bff9f7fae3" xmlns:media="http://search.yahoo.com/mrss/" /></entry><entry xml:lang="en"><title type="html">cmdpeek lets you discover terminal workflows without memorizing aliases</title><link href="http://localhost:4000/2026/07/27/cmd-peek/" rel="alternate" type="text/html" title="cmdpeek lets you discover terminal workflows without memorizing aliases" /><published>2026-07-27T00:00:00+02:00</published><updated>2026-07-27T00:00:00+02:00</updated><id>http://localhost:4000/2026/07/27/cmd-peek</id><content type="html" xml:base="http://localhost:4000/2026/07/27/cmd-peek/"><![CDATA[<p>Shell aliases are useful when you remember them.</p>

<p>The problem starts when your collection grows large enough that you no longer remember:</p>

<ul>
  <li>the exact alias name</li>
  <li>which aliases already exist</li>
  <li>which one solves a specific problem</li>
  <li>what arguments a command expects</li>
  <li>whether a command is safe to run as-is</li>
</ul>

<p>At that point, aliases stop being a productivity tool and start becoming another thing you need to memorize.</p>

<p>That is the problem I wanted to solve with <strong>cmdpeek</strong>.</p>

<p><code class="language-plaintext highlighter-rouge">cmdpeek</code> is an open-source searchable command palette for reusable terminal workflows.</p>

<p>Instead of remembering an exact alias such as:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>klo
</code></pre></div></div>

<p>you can search by what you want to do:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>cmdpeek kubernetes logs
</code></pre></div></div>

<p>The command does not need to match the internal name exactly.</p>

<p><code class="language-plaintext highlighter-rouge">cmdpeek</code> searches across:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>title
name
description
labels
command contents
</code></pre></div></div>

<p>This means a workflow can be discovered using different words, old alias names, technical concepts, or even part of the underlying command.</p>

<blockquote>
  <p><strong>Aliases optimize commands you remember. cmdpeek helps you discover commands you do not.</strong></p>
</blockquote>

<hr />

<h2 id="why-aliases-stop-scaling">Why aliases stop scaling</h2>

<p>A small number of aliases is easy to manage:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">alias </span><span class="nv">k</span><span class="o">=</span><span class="s1">'kubectl'</span>
<span class="nb">alias </span><span class="nv">kgp</span><span class="o">=</span><span class="s1">'kubectl get pods'</span>
<span class="nb">alias </span><span class="nv">klo</span><span class="o">=</span><span class="s1">'kubectl logs -f'</span>
</code></pre></div></div>

<p>But over time, collections often grow into dozens or hundreds of entries covering Kubernetes, Flux, Git, GitHub Actions, AWS, Terraform, local development, troubleshooting, media conversion, and deployment workflows.</p>

<p>The commands may still be useful, but remembering their names becomes difficult.</p>

<p>You may know that you created something to download GitHub Actions logs, force a Flux HelmRelease reconciliation, switch Kubernetes context, inspect Elasticsearch shards, or convert a GIF into MP4.</p>

<p>But you may not remember whether the alias was called:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ghlogs
download-run
gha-debug
flux-force
hr-reconcile
gif2mp4
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">cmdpeek</code> removes that dependency on exact names.</p>

<p>A command can include rich searchable metadata:</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">force-reconcile-flux-helmrelease</span>
  <span class="na">title</span><span class="pi">:</span> <span class="s">Force Flux HelmRelease reconciliation</span>
  <span class="na">description</span><span class="pi">:</span> <span class="s">Select a namespace and HelmRelease, then force Flux to reconcile it with its source.</span>
  <span class="na">labels</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="s">flux</span>
    <span class="pi">-</span> <span class="s">fluxcd</span>
    <span class="pi">-</span> <span class="s">helm</span>
    <span class="pi">-</span> <span class="s">helmrelease</span>
    <span class="pi">-</span> <span class="s">reconcile</span>
    <span class="pi">-</span> <span class="s">force</span>
    <span class="pi">-</span> <span class="s">kubernetes</span>
    <span class="pi">-</span> <span class="s">gitops</span>
    <span class="pi">-</span> <span class="s">troubleshooting</span>
</code></pre></div></div>

<p>That same command can be found with searches such as:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>flux force
helm reconcile
gitops troubleshooting
helmrelease
</code></pre></div></div>

<p>Historical alias names can also remain as labels, so an existing mental shortcut still works without defining a separate shell alias.</p>

<hr />

<h2 id="search-by-intent">Search by intent</h2>

<p>The main idea behind <code class="language-plaintext highlighter-rouge">cmdpeek</code> is simple:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Search by intent
      ↓
Select a workflow
      ↓
Resolve its variables
      ↓
Review the rendered command
      ↓
Confirm and execute
</code></pre></div></div>

<p>The catalog is defined in YAML, making workflows easy to review, version, share, and extend.</p>

<p>A command can include:</p>

<ul>
  <li>a stable internal name</li>
  <li>a human-readable title</li>
  <li>a detailed description</li>
  <li>searchable labels</li>
  <li>a shell script</li>
  <li>interactive variables</li>
  <li>dynamic selectors generated by commands</li>
</ul>

<p>For example:</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">pod-logs</span>
  <span class="na">title</span><span class="pi">:</span> <span class="s">Follow Kubernetes pod logs</span>
  <span class="na">description</span><span class="pi">:</span> <span class="s">Select a namespace and stream logs from one of its pods</span>
  <span class="na">labels</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="s">kubernetes</span>
    <span class="pi">-</span> <span class="s">pods</span>
    <span class="pi">-</span> <span class="s">logs</span>
    <span class="pi">-</span> <span class="s">troubleshooting</span>
    <span class="pi">-</span> <span class="s">stream</span>
    <span class="pi">-</span> <span class="s">klo</span>
  <span class="na">run</span><span class="pi">:</span> <span class="s">kubectl logs -f "" -n ""</span>
</code></pre></div></div>

<p>This workflow can be found through:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>pod logs
kubernetes troubleshooting
stream
klo
</code></pre></div></div>

<p>The exact command name is no longer the only entry point.</p>

<hr />

<h2 id="demo-search-a-large-catalog">Demo: search a large catalog</h2>

<p><img width="2800" height="1800" alt="cmdpeek-search-scroll-demo" src="https://github.com/user-attachments/assets/afc4dbc5-904a-4005-b221-21bff9f7fae3" /></p>

<p>The interactive catalog supports:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>/         Search
↑ / ↓     Navigate
← / →     Change page
e         Open details
Enter     Select
q         Quit
</code></pre></div></div>

<p>Searches are ranked across the command metadata.</p>

<p>Multiple terms can be combined:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>github logs
production deploy
elasticsearch size
kubernetes context
</code></pre></div></div>

<p>A result can therefore match because of its title, description, labels, internal name, or script contents.</p>

<p>This is especially useful for commands that are important but not used frequently enough to memorize.</p>

<hr />

<h2 id="start-with-a-search-directly-from-the-command-line">Start with a search directly from the command line</h2>

<p>You can also pass search terms directly after <code class="language-plaintext highlighter-rouge">cmdpeek</code>:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>cmdpeek kubernetes context
cmdpeek github logs
cmdpeek elasticsearch shards
</code></pre></div></div>

<p>The arguments are joined into an initial search query.</p>

<p>The behavior depends on the number of results:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>exact command name  → open that workflow directly
one search result   → open that workflow directly
multiple results    → show the interactive catalog already filtered
no results          → show the catalog with no matching commands
</code></pre></div></div>

<p>This lets <code class="language-plaintext highlighter-rouge">cmdpeek</code> behave like a discoverable alias system.</p>

<p>When you know roughly what you want, you can start with:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>cmdpeek elastic
</code></pre></div></div>

<p>instead of opening the full catalog and typing the filter manually.</p>

<hr />

<h2 id="demo-direct-search-and-automatic-selection">Demo: direct search and automatic selection</h2>

<p><img width="1800" height="800" alt="cmdpeek-search-demo-filter" src="https://github.com/user-attachments/assets/ec0f0579-82d6-4d92-9e98-63ddb3161705" /></p>

<p>When a search returns several workflows, <code class="language-plaintext highlighter-rouge">cmdpeek</code> opens the filtered list.</p>

<p>When the search returns only one workflow, it skips the list and enters that command directly.</p>

<p>For example:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>cmdpeek gif
</code></pre></div></div>

<p>may directly open a GIF-to-MP4 conversion workflow when it is the only matching command.</p>

<p>This keeps the tool fast when you already know the intent, while preserving discovery when the query is broad.</p>

<hr />

<h2 id="interactive-variables-instead-of-fragile-aliases">Interactive variables instead of fragile aliases</h2>

<p>Aliases become difficult to maintain when commands need parameters.</p>

<p>A shell alias may work for a fixed command:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">alias </span><span class="nv">klo</span><span class="o">=</span><span class="s1">'kubectl logs -f'</span>
</code></pre></div></div>

<p>But real workflows usually need values such as namespace, pod, environment, branch, file path, HelmRelease, GitHub Actions URL, or commit message.</p>

<p><code class="language-plaintext highlighter-rouge">cmdpeek</code> supports four variable sources:</p>

<table>
  <thead>
    <tr>
      <th>Source</th>
      <th>Purpose</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">input</code></td>
      <td>Free-form user input</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">options</code></td>
      <td>Static selectable values</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">environment</code></td>
      <td>Initial value loaded from an environment variable</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">command</code></td>
      <td>Options generated by a shell command</td>
    </tr>
  </tbody>
</table>

<p>A Kubernetes workflow can generate namespaces dynamically:</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">variables</span><span class="pi">:</span>
  <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">namespace</span>
    <span class="na">prompt</span><span class="pi">:</span> <span class="s">Select Kubernetes namespace</span>
    <span class="na">source</span><span class="pi">:</span>
      <span class="na">type</span><span class="pi">:</span> <span class="s">command</span>
      <span class="na">command</span><span class="pi">:</span> <span class="pi">&gt;-</span>
        <span class="s">kubectl get namespaces</span>
        <span class="s">-o jsonpath='{range .items[*]}{.metadata.name}{"\n"}{end}'</span>
</code></pre></div></div>

<p>A second variable can depend on the first:</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code>  <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">pod</span>
    <span class="na">prompt</span><span class="pi">:</span> <span class="s">Select pod</span>
    <span class="na">source</span><span class="pi">:</span>
      <span class="na">type</span><span class="pi">:</span> <span class="s">command</span>
      <span class="na">command</span><span class="pi">:</span> <span class="pi">&gt;-</span>
        <span class="s">kubectl get pods -n ""</span>
        <span class="s">-o jsonpath='{range .items[*]}{.metadata.name}{"\n"}{end}'</span>
</code></pre></div></div>

<p>This turns a command into a guided workflow rather than a string you need to remember and edit manually.</p>

<hr />

<h2 id="dynamic-selectors">Dynamic selectors</h2>

<p>Command-generated variables are especially useful for DevOps workflows.</p>

<p>They can list Kubernetes namespaces, pods, contexts, Flux HelmReleases, Git branches, AWS profiles, files, releases, or environments.</p>

<p>For example, a Flux reconciliation workflow can first list namespaces and then list only the HelmReleases inside the selected namespace:</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">variables</span><span class="pi">:</span>
  <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">namespace</span>
    <span class="na">prompt</span><span class="pi">:</span> <span class="s">Select Kubernetes namespace</span>
    <span class="na">source</span><span class="pi">:</span>
      <span class="na">type</span><span class="pi">:</span> <span class="s">command</span>
      <span class="na">command</span><span class="pi">:</span> <span class="s">kubectl get namespaces -o name</span>

  <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">helmrelease</span>
    <span class="na">prompt</span><span class="pi">:</span> <span class="s">Select Flux HelmRelease</span>
    <span class="na">source</span><span class="pi">:</span>
      <span class="na">type</span><span class="pi">:</span> <span class="s">command</span>
      <span class="na">command</span><span class="pi">:</span> <span class="pi">&gt;-</span>
        <span class="s">flux get helmreleases</span>
        <span class="s">--namespace ""</span>
        <span class="s">--no-header</span>
        <span class="s">| awk '{print $1}'</span>
</code></pre></div></div>

<p>The user does not need to remember or type resource names manually.</p>

<hr />

<h2 id="preview-before-execution">Preview before execution</h2>

<p>Variable values are inserted into the command preview as they are selected.</p>

<p>Long commands and scripts remain scrollable, allowing the final result to be reviewed before execution.</p>

<p>After all variables are resolved, <code class="language-plaintext highlighter-rouge">cmdpeek</code> displays the fully rendered command and requires explicit confirmation:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Execute this command?

╭────────────────────────────────────────────╮
│ flux reconcile helmrelease svc-time       │
│   -n devbox-phoenix-2                     │
│   --with-source                           │
│   --force                                 │
╰────────────────────────────────────────────╯

y execute   n cancel   enter cancel
</code></pre></div></div>

<p>This is particularly useful for commands involving production environments, deployments, deletes, forced reconciliations, pushes, migrations, or infrastructure changes.</p>

<p>The command remains visible before execution instead of being hidden behind an alias.</p>

<hr />

<h2 id="non-interactive-mode">Non-interactive mode</h2>

<p>Interactive discovery is useful when you do not remember the exact workflow.</p>

<p>But sometimes you already know the command name and want to reuse it from a shell function, a script, CI, documentation, or another tool.</p>

<p>For those cases, <code class="language-plaintext highlighter-rouge">cmdpeek</code> also supports non-interactive execution:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>cmdpeek <span class="se">\</span>
  <span class="nt">--no-interactive</span> <span class="se">\</span>
  <span class="nt">--name</span> gcommit <span class="se">\</span>
  <span class="nt">--set</span> <span class="s2">"message=Add non-interactive execution demo"</span>
</code></pre></div></div>

<p>Values can be supplied with repeatable <code class="language-plaintext highlighter-rouge">--set</code> flags:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>cmdpeek <span class="se">\</span>
  <span class="nt">--no-interactive</span> <span class="se">\</span>
  <span class="nt">--name</span> deploy <span class="se">\</span>
  <span class="nt">--set</span> <span class="nv">environment</span><span class="o">=</span>staging <span class="se">\</span>
  <span class="nt">--set</span> <span class="nv">version</span><span class="o">=</span>1.4.2
</code></pre></div></div>

<p>The same YAML catalog can therefore support both interactive discovery and scriptable execution.</p>

<hr />

<h2 id="demo-reuse-a-workflow-non-interactively">Demo: reuse a workflow non-interactively</h2>

<p><img width="1800" height="800" alt="cmdpeek-non-interactive-gcommit" src="https://github.com/user-attachments/assets/bf6ea668-159a-4379-b0ed-fe233c17c92e" /></p>

<p>This mode still renders the command and asks for confirmation unless <code class="language-plaintext highlighter-rouge">--yes</code> is provided.</p>

<p>A dry run is also available:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>cmdpeek <span class="se">\</span>
  <span class="nt">--no-interactive</span> <span class="se">\</span>
  <span class="nt">--name</span> gcommit <span class="se">\</span>
  <span class="nt">--set</span> <span class="s2">"message=Preview this commit"</span> <span class="se">\</span>
  <span class="nt">--dry-run</span>
</code></pre></div></div>

<p>This prints the final command without executing it.</p>

<hr />

<h2 id="local-and-shared-catalogs">Local and shared catalogs</h2>

<p>By default, <code class="language-plaintext highlighter-rouge">cmdpeek</code> looks for:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>.cmdpeek.yaml
</code></pre></div></div>

<p>A local path can be configured through:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">export </span><span class="nv">CMDPEEK_CONFIG_FILE</span><span class="o">=</span><span class="s2">"</span><span class="nv">$HOME</span><span class="s2">/.config/cmdpeek/commands.yaml"</span>
</code></pre></div></div>

<p>Catalogs can also be loaded from GitHub:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">export </span><span class="nv">CMDPEEK_CONFIG_GITHUB</span><span class="o">=</span><span class="s2">"company/platform-config:cmdpeek/commands.yaml@main"</span>
</code></pre></div></div>

<p>For private repositories, authentication can be supplied through:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">export </span><span class="nv">CMDPEEK_GITHUB_TOKEN</span><span class="o">=</span><span class="s2">"github_pat_..."</span>
</code></pre></div></div>

<p>Remote configurations are cached locally and refreshed using GitHub ETags.</p>

<p>This makes it possible to maintain a personal workflow catalog, a repository-specific catalog, a shared team catalog, or a versioned platform engineering catalog.</p>

<p>The YAML file becomes executable documentation for workflows that would otherwise be scattered across aliases, shell history, snippets, and README files.</p>

<hr />

<h2 id="example-workflows">Example workflows</h2>

<p>The same catalog can describe very different tasks.</p>

<h3 id="analyze-github-actions-logs">Analyze GitHub Actions logs</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>cmdpeek github logs
</code></pre></div></div>

<p>The workflow can request a GitHub Actions run or job URL, download the logs, extract them, and prepare them for analysis.</p>

<h3 id="force-a-flux-helmrelease-reconciliation">Force a Flux HelmRelease reconciliation</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>cmdpeek flux force
</code></pre></div></div>

<p>The workflow can interactively select a namespace and HelmRelease before running:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>flux reconcile helmrelease &lt;name&gt; <span class="se">\</span>
  <span class="nt">--namespace</span> &lt;namespace&gt; <span class="se">\</span>
  <span class="nt">--with-source</span> <span class="se">\</span>
  <span class="nt">--force</span>
</code></pre></div></div>

<h3 id="switch-kubernetes-context">Switch Kubernetes context</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>cmdpeek kubernetes context
</code></pre></div></div>

<p>Available contexts can be generated dynamically from the current kubeconfig.</p>

<h3 id="convert-gif-to-mp4">Convert GIF to MP4</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>cmdpeek gif mp4
</code></pre></div></div>

<p>The workflow can request input and output paths and execute a consistent <code class="language-plaintext highlighter-rouge">ffmpeg</code> conversion.</p>

<h3 id="run-a-git-commit-workflow">Run a Git commit workflow</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>cmdpeek <span class="se">\</span>
  <span class="nt">--no-interactive</span> <span class="se">\</span>
  <span class="nt">--name</span> gcommit <span class="se">\</span>
  <span class="nt">--set</span> <span class="s2">"message=Document cmdpeek workflows"</span>
</code></pre></div></div>

<hr />

<h2 id="installation">Installation</h2>

<h3 id="homebrew">Homebrew</h3>

<p>Linux and macOS:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>brew tap pierinho13/tools
brew <span class="nb">install</span> <span class="nt">--cask</span> cmdpeek
</code></pre></div></div>

<p>Upgrade:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>brew update
brew upgrade <span class="nt">--cask</span> cmdpeek
</code></pre></div></div>

<h3 id="github-releases">GitHub Releases</h3>

<p>Precompiled binaries are available through GitHub Releases.</p>

<h3 id="build-from-source">Build from source</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git clone https://github.com/pierinho13/cmdpeek.git
<span class="nb">cd </span>cmdpeek

go build <span class="nt">-o</span> cmdpeek ./cmd/cmdpeek
<span class="nb">sudo mv </span>cmdpeek /usr/local/bin/
</code></pre></div></div>

<hr />

<h2 id="quick-start">Quick start</h2>

<p>Create <code class="language-plaintext highlighter-rouge">.cmdpeek.yaml</code>:</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">version</span><span class="pi">:</span> <span class="m">1</span>
<span class="na">shell</span><span class="pi">:</span> <span class="s">bash</span>

<span class="na">commands</span><span class="pi">:</span>
  <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">greet</span>
    <span class="na">title</span><span class="pi">:</span> <span class="s">Greet a person</span>
    <span class="na">description</span><span class="pi">:</span> <span class="s">Print a personalized greeting</span>
    <span class="na">labels</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s">example</span>
      <span class="pi">-</span> <span class="s">greeting</span>
      <span class="pi">-</span> <span class="s">hello</span>
    <span class="na">run</span><span class="pi">:</span> <span class="pi">|</span>
      <span class="s">name=""</span>
      <span class="s">echo "Hello, ${name}!"</span>
    <span class="na">variables</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">name</span>
        <span class="na">prompt</span><span class="pi">:</span> <span class="s">Person name</span>
        <span class="na">default</span><span class="pi">:</span> <span class="s">world</span>
        <span class="na">source</span><span class="pi">:</span>
          <span class="na">type</span><span class="pi">:</span> <span class="s">input</span>
</code></pre></div></div>

<p>Then run:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>cmdpeek
</code></pre></div></div>

<p>Or search directly:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>cmdpeek greeting
</code></pre></div></div>

<hr />

<h2 id="why-not-just-use-aliases-fzf-or-a-task-runner">Why not just use aliases, fzf, or a task runner?</h2>

<p><code class="language-plaintext highlighter-rouge">cmdpeek</code> is not intended to replace every shell alias, fuzzy finder, or task runner.</p>

<p>Aliases are still ideal for very short commands you use constantly and remember easily.</p>

<p>Task runners are excellent for repository-defined build and automation tasks.</p>

<p><code class="language-plaintext highlighter-rouge">fzf</code> is excellent for filtering arbitrary streams of text.</p>

<p>The focus of <code class="language-plaintext highlighter-rouge">cmdpeek</code> is different:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>structured command discovery by intent
+
guided variable resolution
+
live preview
+
safe execution
</code></pre></div></div>

<p>The searchable metadata belongs to the command model itself.</p>

<p>A workflow is not just a command string. It has a title, description, labels, variables, and execution behavior.</p>

<p>That makes it easier to discover months later, share with other people, and evolve without creating more aliases.</p>

<hr />

<h2 id="final-thoughts">Final thoughts</h2>

<p>The main goal of <code class="language-plaintext highlighter-rouge">cmdpeek</code> is not simply to execute commands.</p>

<p>The goal is to make useful terminal workflows discoverable.</p>

<p>A command you cannot remember effectively does not exist when you need it.</p>

<p>By searching names, titles, descriptions, labels, and command contents, <code class="language-plaintext highlighter-rouge">cmdpeek</code> makes it possible to find workflows using the words you remember at that moment.</p>

<p>That is what makes it different from a large alias file.</p>

<p>Instead of asking:</p>

<blockquote>
  <p>What did I call that alias?</p>
</blockquote>

<p>You can ask:</p>

<blockquote>
  <p>What do I want to do?</p>
</blockquote>

<p>The project is open source:</p>

<p><strong>https://github.com/pierinho13/cmdpeek</strong></p>

<p>Contributions, feedback, workflow examples, documentation improvements, tests, packaging support, and TUI ideas are very welcome.</p>

<p>If you’re interested in this kind of platform engineering work, you can learn more about my experience or <a href="/en/contact">get in touch</a>.</p>]]></content><author><name></name></author><summary type="html"><![CDATA[cmdpeek is a searchable command palette for reusable terminal workflows. It lets you find commands by intent using names, titles, descriptions, labels, and command contents, then resolve variables, preview the final command, and execute it safely.]]></summary><media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="https://github.com/user-attachments/assets/afc4dbc5-904a-4005-b221-21bff9f7fae3" /><media:content medium="image" url="https://github.com/user-attachments/assets/afc4dbc5-904a-4005-b221-21bff9f7fae3" xmlns:media="http://search.yahoo.com/mrss/" /></entry><entry xml:lang="en"><title type="html">kubectl-peek now opens isolated Kubernetes shells for any context and namespace</title><link href="http://localhost:4000/2026/07/20/kubectl-peek-isolated-kubernetes-shells/" rel="alternate" type="text/html" title="kubectl-peek now opens isolated Kubernetes shells for any context and namespace" /><published>2026-07-20T00:00:00+02:00</published><updated>2026-07-20T00:00:00+02:00</updated><id>http://localhost:4000/2026/07/20/kubectl-peek-isolated-kubernetes-shells</id><content type="html" xml:base="http://localhost:4000/2026/07/20/kubectl-peek-isolated-kubernetes-shells/"><![CDATA[<p>Switching between Kubernetes clusters and namespaces is one of those small tasks that becomes surprisingly expensive when you do it all day.</p>

<p>A typical workflow looks like this:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl config use-context staging
kubectl config set-context <span class="nt">--current</span> <span class="nt">--namespace</span> payments
kubectl get pods
</code></pre></div></div>

<p>Then a few minutes later:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl config use-context production
kubectl config set-context <span class="nt">--current</span> <span class="nt">--namespace</span> payments
kubectl get pods
</code></pre></div></div>

<p>This works, but it also changes your real kubeconfig state.</p>

<p>That creates several familiar problems:</p>

<ul>
  <li>you forget which context is currently active</li>
  <li>a new terminal inherits a context changed somewhere else</li>
  <li>you keep passing <code class="language-plaintext highlighter-rouge">--context</code> and <code class="language-plaintext highlighter-rouge">--namespace</code> to every command</li>
  <li>you risk running a command against the wrong cluster</li>
  <li>parallel work across environments becomes awkward</li>
  <li>tools such as Helm and Flux need the same scope repeated again</li>
</ul>

<p>The newest version of <strong>kubectl-peek</strong> solves this with isolated Kubernetes shells.</p>

<p>You can now select a context and namespace, open a temporary shell scoped to both, work normally, and return to your original environment with a simple <code class="language-plaintext highlighter-rouge">exit</code>.</p>

<p>No permanent context switch.</p>

<p>No modification to your original kubeconfig.</p>

<p>No need to repeat flags on every command.</p>

<hr />

<h2 id="kubectl-peek-is-becoming-a-broader-kubernetes-productivity-cli">kubectl-peek is becoming a broader Kubernetes productivity CLI</h2>

<p><code class="language-plaintext highlighter-rouge">kubectl-peek</code> originally started as an interactive way to inspect Kubernetes Secrets and discover which resources use, produce, or reference them.</p>

<p>That functionality is still available through:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl-peek secret
</code></pre></div></div>

<p>But the project now covers several first-class Kubernetes workflows:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl-peek
├── secret       Inspect Secrets and their relationships
├── namespace    Select and persist a namespace
└── shell        Open an isolated context-aware shell
</code></pre></div></div>

<p>Running the root command now shows the available workflows:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl-peek
</code></pre></div></div>

<p>The same commands also work as a native <code class="language-plaintext highlighter-rouge">kubectl</code> plugin:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl peek secret
kubectl peek namespace
kubectl peek shell
</code></pre></div></div>

<hr />

<h2 id="the-common-problem-context-switching-changes-global-state">The common problem: context switching changes global state</h2>

<p>Kubernetes contexts are convenient because they combine:</p>

<ul>
  <li>a cluster</li>
  <li>user credentials</li>
  <li>a default namespace</li>
</ul>

<p>But changing the active context also changes the state used by your normal terminal session.</p>

<p>For example:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl config use-context production
</code></pre></div></div>

<p>After that, every command that relies on the default kubeconfig starts targeting production unless you explicitly override it.</p>

<p>The same applies to namespaces:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl config set-context <span class="nt">--current</span> <span class="nt">--namespace</span> payments
</code></pre></div></div>

<p>This is especially inconvenient when you need to compare environments or work in several clusters at the same time.</p>

<p>You can avoid changing context by passing flags:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl <span class="se">\</span>
  <span class="nt">--context</span> production <span class="se">\</span>
  <span class="nt">--namespace</span> payments <span class="se">\</span>
  get pods
</code></pre></div></div>

<p>But then every command becomes repetitive:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl <span class="nt">--context</span> production <span class="nt">-n</span> payments get pods
helm <span class="nt">--kube-context</span> production <span class="nt">-n</span> payments list
flux <span class="nt">--context</span> production <span class="nt">-n</span> payments get all
</code></pre></div></div>

<p>The isolated shell workflow gives you the convenience of a selected context and namespace without changing your real kubeconfig.</p>

<hr />

<h2 id="open-an-isolated-kubernetes-shell">Open an isolated Kubernetes shell</h2>

<p>Run:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl-peek shell
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">kubectl-peek</code> will:</p>

<ol>
  <li>list the contexts available in your kubeconfig</li>
  <li>let you select one interactively</li>
  <li>connect through that context</li>
  <li>list the namespaces available in that cluster</li>
  <li>let you select a namespace</li>
  <li>create a temporary kubeconfig</li>
  <li>open a child shell using that temporary configuration</li>
</ol>

<p>The full workflow looks like this:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Select a Kubernetes context
            │
            ▼
List namespaces available through that context
            │
            ▼
Select a namespace
            │
            ▼
Create a temporary kubeconfig
            │
            ▼
Open an isolated shell
</code></pre></div></div>

<hr />

<h2 id="demo">Demo</h2>

<p><img width="1800" height="800" alt="kubectl-peek isolated Kubernetes shell demo" src="https://github.com/user-attachments/assets/9eb0f303-cc02-4cc2-b434-ead0ceb10c35" /></p>

<p>Inside the child shell, the selected context and namespace remain visible in the prompt:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[k8s:production ns:payments] piero@MacBook-Pro %
</code></pre></div></div>

<p>The context is displayed in cyan and the namespace in yellow on terminals that support ANSI colors.</p>

<p>That small visual indicator makes a real difference when several terminals are open at the same time.</p>

<hr />

<h2 id="skip-the-interactive-selectors-when-you-already-know-the-target">Skip the interactive selectors when you already know the target</h2>

<p>You can provide the context directly:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl-peek shell <span class="nt">--context</span> production
</code></pre></div></div>

<p>In that case, only the namespace selector is displayed.</p>

<p>You can also provide the namespace:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl-peek shell <span class="nt">-n</span> payments
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">kubectl-peek</code> will ask for the context and validate that the namespace exists in the selected cluster.</p>

<p>To skip both selectors:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl-peek shell <span class="se">\</span>
  <span class="nt">--context</span> production <span class="se">\</span>
  <span class="nt">--namespace</span> payments
</code></pre></div></div>

<p>This is useful for aliases, scripts, documentation, or environments you use frequently.</p>

<hr />

<h2 id="work-normally-inside-the-shell">Work normally inside the shell</h2>

<p>Once the isolated shell is open, you can use your usual Kubernetes tools without repeating context or namespace options:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get pods
kubectl get services
kubectl describe deployment api
helm list
flux get all
</code></pre></div></div>

<p>All of those commands use the temporary kubeconfig exposed to the child shell through <code class="language-plaintext highlighter-rouge">KUBECONFIG</code>.</p>

<p>The parent terminal remains unchanged.</p>

<hr />

<h2 id="the-original-kubeconfig-is-not-modified">The original kubeconfig is not modified</h2>

<p>The temporary kubeconfig is created specifically for the child shell.</p>

<p>It:</p>

<ul>
  <li>contains the effective Kubernetes configuration</li>
  <li>preserves clusters, users, contexts, credentials, and exec plugins</li>
  <li>embeds certificate and key data referenced through local file paths</li>
  <li>selects the requested context</li>
  <li>applies the selected namespace only to that context</li>
  <li>is created with <code class="language-plaintext highlighter-rouge">0600</code> permissions</li>
  <li>is exposed only inside the child shell</li>
  <li>is removed after leaving the shell</li>
</ul>

<p>The original kubeconfig remains untouched.</p>

<p>When you finish:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">exit</span>
</code></pre></div></div>

<p>you return to the previous terminal with the same Kubernetes configuration you had before.</p>

<hr />

<h2 id="work-across-multiple-clusters-in-parallel">Work across multiple clusters in parallel</h2>

<p>This is one of the most useful parts of the new workflow.</p>

<p>Open one terminal for staging:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl-peek shell <span class="se">\</span>
  <span class="nt">--context</span> staging <span class="se">\</span>
  <span class="nt">--namespace</span> payments
</code></pre></div></div>

<p>Open another for production:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl-peek shell <span class="se">\</span>
  <span class="nt">--context</span> production <span class="se">\</span>
  <span class="nt">--namespace</span> payments
</code></pre></div></div>

<p>Now each terminal has its own temporary Kubernetes scope:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[k8s:staging ns:payments]
</code></pre></div></div>

<p>and:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[k8s:production ns:payments]
</code></pre></div></div>

<p>You can compare logs, resources, Helm releases, Flux state, or deployments without repeatedly switching your shared context.</p>

<hr />

<h2 id="nested-isolated-shells-are-blocked">Nested isolated shells are blocked</h2>

<p>Opening isolated shells inside isolated shells can become confusing very quickly.</p>

<p>For that reason, <code class="language-plaintext highlighter-rouge">kubectl-peek</code> detects an active isolated shell before displaying any selectors.</p>

<p>Attempting to open another one returns an error similar to:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>an isolated kubectl-peek shell is already active
(context "production", namespace "payments");
run exit before opening another one
</code></pre></div></div>

<p>The check happens early, before selecting another context or namespace.</p>

<hr />

<h2 id="namespace-first-workflow">Namespace-first workflow</h2>

<p>The existing namespace selector can also open an isolated shell:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl-peek namespace <span class="nt">--shell</span>
</code></pre></div></div>

<p>This workflow is useful when you want to keep the current context and only select a namespace.</p>

<p>You can provide an initial filter:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl-peek namespace pay <span class="nt">--shell</span>
</code></pre></div></div>

<p>Or select another context explicitly:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl-peek namespace <span class="se">\</span>
  <span class="nt">--context</span> staging <span class="se">\</span>
  <span class="nt">--shell</span>
</code></pre></div></div>

<p>Without <code class="language-plaintext highlighter-rouge">--shell</code>, the command persists the selected namespace in the chosen kubeconfig context:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl-peek namespace
</code></pre></div></div>

<p>That gives you both options:</p>

<ul>
  <li>persist a namespace intentionally</li>
  <li>open an ephemeral namespace-scoped shell</li>
</ul>

<hr />

<h2 id="secret-inspection-is-now-an-explicit-command">Secret inspection is now an explicit command</h2>

<p>As <code class="language-plaintext highlighter-rouge">kubectl-peek</code> has grown beyond Secrets, Secret inspection has moved into its own first-class command:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl-peek secret
</code></pre></div></div>

<p>Filter by name:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl-peek secret database
</code></pre></div></div>

<p>Inspect another namespace:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl-peek secret database <span class="nt">-n</span> staging
</code></pre></div></div>

<p>Load custom CRD discovery rules:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl-peek secret <span class="se">\</span>
  <span class="nt">--rules</span> ./examples/rules-all.yaml
</code></pre></div></div>

<p>Secret inspection still includes:</p>

<ul>
  <li>interactive selection</li>
  <li>filtering and pagination</li>
  <li>decoded values</li>
  <li>built-in relationship discovery</li>
  <li>custom CRD discovery through YAML rules</li>
  <li><code class="language-plaintext highlighter-rouge">uses</code>, <code class="language-plaintext highlighter-rouge">produces</code>, and <code class="language-plaintext highlighter-rouge">references</code> relationships</li>
</ul>

<p>The difference is that the CLI now presents Secrets, namespaces, and isolated shells as equally important workflows.</p>

<hr />

<h2 id="why-temporary-kubeconfigs-instead-of-changing-the-process-environment-only">Why temporary kubeconfigs instead of changing the process environment only?</h2>

<p>Setting a namespace environment variable would not be enough.</p>

<p>Kubernetes tools do not all use the same flags or environment variables, but they do understand kubeconfig.</p>

<p>By creating a temporary kubeconfig and exposing it through <code class="language-plaintext highlighter-rouge">KUBECONFIG</code>, the isolated scope works naturally with tools such as:</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">kubectl</code></li>
  <li>Helm</li>
  <li>Flux</li>
  <li>Kustomize workflows that invoke Kubernetes clients</li>
  <li>Go applications using <code class="language-plaintext highlighter-rouge">client-go</code></li>
  <li>other tools that respect kubeconfig</li>
</ul>

<p>This makes the shell useful beyond a single <code class="language-plaintext highlighter-rouge">kubectl</code> command.</p>

<hr />

<h2 id="installation">Installation</h2>

<h3 id="homebrew">Homebrew</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>brew tap pierinho13/tools
brew <span class="nb">install</span> <span class="nt">--cask</span> kubectl-peek
</code></pre></div></div>

<p>Upgrade an existing installation:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>brew update
brew upgrade <span class="nt">--cask</span> kubectl-peek
</code></pre></div></div>

<p>Verify the installed version:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl-peek <span class="nt">--version</span>
</code></pre></div></div>

<hr />

<h3 id="github-releases">GitHub Releases</h3>

<p>Precompiled binaries are available for macOS, Linux, and Windows through GitHub Releases.</p>

<hr />

<h3 id="build-from-source">Build from source</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git clone https://github.com/pierinho13/kubectl-peek.git
<span class="nb">cd </span>kubectl-peek
go build <span class="nt">-o</span> kubectl-peek <span class="nb">.</span>
</code></pre></div></div>

<hr />

<h2 id="example-workflows">Example workflows</h2>

<h3 id="select-everything-interactively">Select everything interactively</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl-peek shell
</code></pre></div></div>

<h3 id="select-only-the-namespace">Select only the namespace</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl-peek shell <span class="nt">--context</span> production
</code></pre></div></div>

<h3 id="open-directly">Open directly</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl-peek shell <span class="nt">--context</span> production <span class="nt">-n</span> payments
</code></pre></div></div>

<h3 id="keep-the-current-context-and-select-a-temporary-namespace">Keep the current context and select a temporary namespace</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl-peek namespace <span class="nt">--shell</span>
</code></pre></div></div>

<h3 id="persist-a-namespace-in-the-current-context">Persist a namespace in the current context</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl-peek namespace
</code></pre></div></div>

<h3 id="inspect-secret-relationships">Inspect Secret relationships</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl-peek secret
</code></pre></div></div>

<hr />

<h2 id="final-thoughts">Final thoughts</h2>

<p>The goal of <code class="language-plaintext highlighter-rouge">kubectl-peek</code> is not to replace <code class="language-plaintext highlighter-rouge">kubectl</code>.</p>

<p>It is to remove friction from a few Kubernetes workflows that platform engineers, SREs, and developers repeat constantly.</p>

<p>Secret inspection helps answer:</p>

<blockquote>
  <p>What is this Secret connected to?</p>
</blockquote>

<p>The new shell workflow helps answer:</p>

<blockquote>
  <p>How can I work safely in this context and namespace without changing my real environment?</p>
</blockquote>

<p>That makes <code class="language-plaintext highlighter-rouge">kubectl-peek</code> useful not only when investigating Secrets, but also during deployments, incident response, cluster comparisons, namespace troubleshooting, and everyday multi-cluster work.</p>

<p>The project is open source:</p>

<p><strong>https://github.com/pierinho13/kubectl-peek</strong></p>

<p>Contributions, feedback, and ideas are welcome.</p>

<p>If you’re interested in this kind of platform engineering work, you can learn more about my experience or <a href="/en/contact">get in touch</a>.</p>]]></content><author><name></name></author><summary type="html"><![CDATA[kubectl-peek now lets you open isolated, context-aware Kubernetes shells backed by temporary kubeconfigs, so you can work across clusters and namespaces without changing your real context or repeating flags.]]></summary><media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="https://github.com/user-attachments/assets/9eb0f303-cc02-4cc2-b434-ead0ceb10c35" /><media:content medium="image" url="https://github.com/user-attachments/assets/9eb0f303-cc02-4cc2-b434-ead0ceb10c35" xmlns:media="http://search.yahoo.com/mrss/" /></entry><entry xml:lang="es"><title type="html">kubectl-peek ahora abre shells aisladas de Kubernetes para cualquier contexto y namespace</title><link href="http://localhost:4000/2026/07/20/kubectl-peek-isolated-kubernetes-shells-es/" rel="alternate" type="text/html" title="kubectl-peek ahora abre shells aisladas de Kubernetes para cualquier contexto y namespace" /><published>2026-07-20T00:00:00+02:00</published><updated>2026-07-20T00:00:00+02:00</updated><id>http://localhost:4000/2026/07/20/kubectl-peek-isolated-kubernetes-shells-es</id><content type="html" xml:base="http://localhost:4000/2026/07/20/kubectl-peek-isolated-kubernetes-shells-es/"><![CDATA[<p>Cambiar entre clusters y namespaces de Kubernetes es una de esas tareas pequeñas que acaba consumiendo bastante tiempo cuando se repite durante todo el día.</p>

<p>Un flujo habitual suele ser así:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl config use-context staging
kubectl config set-context <span class="nt">--current</span> <span class="nt">--namespace</span> payments
kubectl get pods
</code></pre></div></div>

<p>Y unos minutos después:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl config use-context production
kubectl config set-context <span class="nt">--current</span> <span class="nt">--namespace</span> payments
kubectl get pods
</code></pre></div></div>

<p>Esto funciona, pero también modifica el estado real de tu kubeconfig.</p>

<p>Y eso genera varios problemas muy comunes:</p>

<ul>
  <li>olvidas qué contexto está activo</li>
  <li>una terminal nueva hereda un contexto cambiado en otro momento</li>
  <li>tienes que pasar <code class="language-plaintext highlighter-rouge">--context</code> y <code class="language-plaintext highlighter-rouge">--namespace</code> en cada comando</li>
  <li>corres el riesgo de ejecutar una acción contra el cluster equivocado</li>
  <li>trabajar en paralelo con varios entornos resulta incómodo</li>
  <li>herramientas como Helm y Flux necesitan que repitas el mismo alcance</li>
</ul>

<p>La nueva versión de <strong>kubectl-peek</strong> soluciona este problema mediante shells aisladas de Kubernetes.</p>

<p>Ahora puedes seleccionar un contexto y un namespace, abrir una shell temporal limitada a ambos, trabajar con normalidad y regresar a tu entorno original ejecutando simplemente:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">exit</span>
</code></pre></div></div>

<p>Sin cambiar permanentemente el contexto.</p>

<p>Sin modificar el kubeconfig original.</p>

<p>Sin repetir flags en cada comando.</p>

<hr />

<h2 id="kubectl-peek-está-evolucionando-hacia-una-cli-de-productividad-para-kubernetes">kubectl-peek está evolucionando hacia una CLI de productividad para Kubernetes</h2>

<p><code class="language-plaintext highlighter-rouge">kubectl-peek</code> nació inicialmente como una forma interactiva de inspeccionar Secrets de Kubernetes y descubrir qué recursos los usan, producen o referencian.</p>

<p>Esa funcionalidad sigue disponible mediante:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl-peek secret
</code></pre></div></div>

<p>Pero el proyecto ahora cubre varios flujos de Kubernetes como funcionalidades principales:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl-peek
├── secret       Inspecciona Secrets y sus relaciones
├── namespace    Selecciona y guarda un namespace
└── shell        Abre una shell aislada por contexto y namespace
</code></pre></div></div>

<p>Al ejecutar el comando raíz se muestran todos los flujos disponibles:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl-peek
</code></pre></div></div>

<p>Los mismos comandos también funcionan como plugin nativo de <code class="language-plaintext highlighter-rouge">kubectl</code>:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl peek secret
kubectl peek namespace
kubectl peek shell
</code></pre></div></div>

<hr />

<h2 id="el-problema-habitual-cambiar-de-contexto-modifica-un-estado-compartido">El problema habitual: cambiar de contexto modifica un estado compartido</h2>

<p>Los contextos de Kubernetes son útiles porque combinan:</p>

<ul>
  <li>un cluster</li>
  <li>unas credenciales</li>
  <li>un namespace predeterminado</li>
</ul>

<p>Pero cambiar el contexto activo también modifica el estado utilizado por tu terminal normal.</p>

<p>Por ejemplo:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl config use-context production
</code></pre></div></div>

<p>A partir de ese momento, todos los comandos que usen el kubeconfig predeterminado apuntarán a producción, salvo que indiques explícitamente otro contexto.</p>

<p>Lo mismo ocurre con los namespaces:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl config set-context <span class="nt">--current</span> <span class="nt">--namespace</span> payments
</code></pre></div></div>

<p>Esto resulta especialmente incómodo cuando necesitas comparar entornos o trabajar con varios clusters al mismo tiempo.</p>

<p>Puedes evitar cambiar el contexto pasando flags:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl   <span class="nt">--context</span> production   <span class="nt">--namespace</span> payments   get pods
</code></pre></div></div>

<p>Pero entonces cada comando se vuelve repetitivo:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl <span class="nt">--context</span> production <span class="nt">-n</span> payments get pods
helm <span class="nt">--kube-context</span> production <span class="nt">-n</span> payments list
flux <span class="nt">--context</span> production <span class="nt">-n</span> payments get all
</code></pre></div></div>

<p>El flujo de shells aisladas ofrece la comodidad de tener un contexto y namespace seleccionados sin modificar tu kubeconfig real.</p>

<hr />

<h2 id="abrir-una-shell-aislada-de-kubernetes">Abrir una shell aislada de Kubernetes</h2>

<p>Ejecuta:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl-peek shell
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">kubectl-peek</code> hará lo siguiente:</p>

<ol>
  <li>listará los contextos disponibles en tu kubeconfig</li>
  <li>permitirá seleccionar uno de forma interactiva</li>
  <li>se conectará utilizando ese contexto</li>
  <li>listará los namespaces disponibles en ese cluster</li>
  <li>permitirá seleccionar un namespace</li>
  <li>creará un kubeconfig temporal</li>
  <li>abrirá una shell hija utilizando esa configuración temporal</li>
</ol>

<p>El flujo completo es:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Seleccionar un contexto de Kubernetes
                 │
                 ▼
Listar los namespaces disponibles en ese contexto
                 │
                 ▼
Seleccionar un namespace
                 │
                 ▼
Crear un kubeconfig temporal
                 │
                 ▼
Abrir una shell aislada
</code></pre></div></div>

<hr />

<h2 id="demostración">Demostración</h2>

<p><img width="1800" height="800" alt="Demostración de shell aislada de Kubernetes con kubectl-peek" src="https://github.com/user-attachments/assets/9eb0f303-cc02-4cc2-b434-ead0ceb10c35" /></p>

<p>Dentro de la shell hija, el contexto y el namespace seleccionados permanecen visibles en el prompt:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[k8s:production ns:payments] piero@MacBook-Pro %
</code></pre></div></div>

<p>El contexto se muestra en cian y el namespace en amarillo en terminales compatibles con colores ANSI.</p>

<p>Ese pequeño indicador visual supone una gran diferencia cuando tienes varias terminales abiertas al mismo tiempo.</p>

<hr />

<h2 id="evitar-los-selectores-cuando-ya-conoces-el-destino">Evitar los selectores cuando ya conoces el destino</h2>

<p>Puedes indicar directamente el contexto:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl-peek shell <span class="nt">--context</span> production
</code></pre></div></div>

<p>En ese caso, solo se mostrará el selector de namespaces.</p>

<p>También puedes indicar directamente el namespace:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl-peek shell <span class="nt">-n</span> payments
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">kubectl-peek</code> solicitará el contexto y comprobará que el namespace exista en el cluster seleccionado.</p>

<p>Para evitar ambos selectores:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl-peek shell   <span class="nt">--context</span> production   <span class="nt">--namespace</span> payments
</code></pre></div></div>

<p>Esto resulta útil para aliases, documentación, scripts o entornos que utilizas frecuentemente.</p>

<hr />

<h2 id="trabajar-con-normalidad-dentro-de-la-shell">Trabajar con normalidad dentro de la shell</h2>

<p>Una vez abierta la shell aislada, puedes utilizar tus herramientas habituales de Kubernetes sin repetir el contexto ni el namespace:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get pods
kubectl get services
kubectl describe deployment api
helm list
flux get all
</code></pre></div></div>

<p>Todos esos comandos utilizan el kubeconfig temporal expuesto a la shell hija mediante <code class="language-plaintext highlighter-rouge">KUBECONFIG</code>.</p>

<p>La terminal principal permanece sin cambios.</p>

<hr />

<h2 id="el-kubeconfig-original-no-se-modifica">El kubeconfig original no se modifica</h2>

<p>El kubeconfig temporal se crea específicamente para la shell hija.</p>

<p>Este kubeconfig:</p>

<ul>
  <li>contiene la configuración efectiva de Kubernetes</li>
  <li>conserva clusters, usuarios, contextos, credenciales y plugins de autenticación</li>
  <li>incrusta certificados y claves referenciados mediante rutas locales</li>
  <li>selecciona el contexto solicitado</li>
  <li>aplica el namespace seleccionado únicamente a ese contexto</li>
  <li>se crea con permisos <code class="language-plaintext highlighter-rouge">0600</code></li>
  <li>se expone solamente dentro de la shell hija</li>
  <li>se elimina después de abandonar la shell</li>
</ul>

<p>El kubeconfig original permanece intacto.</p>

<p>Cuando terminas:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">exit</span>
</code></pre></div></div>

<p>regresas a la terminal anterior con la misma configuración de Kubernetes que tenías antes.</p>

<hr />

<h2 id="trabajar-con-varios-clusters-en-paralelo">Trabajar con varios clusters en paralelo</h2>

<p>Esta es una de las ventajas más útiles del nuevo flujo.</p>

<p>Abre una terminal para staging:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl-peek shell   <span class="nt">--context</span> staging   <span class="nt">--namespace</span> payments
</code></pre></div></div>

<p>Y otra terminal para producción:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl-peek shell   <span class="nt">--context</span> production   <span class="nt">--namespace</span> payments
</code></pre></div></div>

<p>Cada terminal mantiene su propio alcance temporal de Kubernetes:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[k8s:staging ns:payments]
</code></pre></div></div>

<p>y:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[k8s:production ns:payments]
</code></pre></div></div>

<p>Puedes comparar logs, recursos, releases de Helm, estado de Flux o despliegues sin cambiar constantemente un contexto compartido.</p>

<hr />

<h2 id="las-shells-aisladas-anidadas-están-bloqueadas">Las shells aisladas anidadas están bloqueadas</h2>

<p>Abrir una shell aislada dentro de otra puede resultar confuso rápidamente.</p>

<p>Por eso, <code class="language-plaintext highlighter-rouge">kubectl-peek</code> detecta si ya existe una shell aislada activa antes de mostrar cualquier selector.</p>

<p>Intentar abrir otra devuelve un error similar a:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>an isolated kubectl-peek shell is already active
(context "production", namespace "payments");
run exit before opening another one
</code></pre></div></div>

<p>La comprobación se realiza al principio, antes de seleccionar otro contexto o namespace.</p>

<hr />

<h2 id="flujo-basado-primero-en-el-namespace">Flujo basado primero en el namespace</h2>

<p>El selector de namespaces también puede abrir una shell aislada:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl-peek namespace <span class="nt">--shell</span>
</code></pre></div></div>

<p>Este flujo resulta útil cuando quieres mantener el contexto actual y seleccionar únicamente el namespace.</p>

<p>Puedes proporcionar un filtro inicial:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl-peek namespace pay <span class="nt">--shell</span>
</code></pre></div></div>

<p>O indicar explícitamente otro contexto:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl-peek namespace   <span class="nt">--context</span> staging   <span class="nt">--shell</span>
</code></pre></div></div>

<p>Sin <code class="language-plaintext highlighter-rouge">--shell</code>, el comando guarda el namespace seleccionado en el contexto elegido del kubeconfig:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl-peek namespace
</code></pre></div></div>

<p>De esta forma dispones de ambas opciones:</p>

<ul>
  <li>guardar un namespace de manera intencionada</li>
  <li>abrir una shell efímera limitada a un namespace</li>
</ul>

<hr />

<h2 id="la-inspección-de-secrets-ahora-es-un-comando-explícito">La inspección de Secrets ahora es un comando explícito</h2>

<p>Como <code class="language-plaintext highlighter-rouge">kubectl-peek</code> ha crecido más allá de la inspección de Secrets, esta funcionalidad ahora dispone de su propio comando principal:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl-peek secret
</code></pre></div></div>

<p>Filtrar por nombre:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl-peek secret database
</code></pre></div></div>

<p>Inspeccionar otro namespace:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl-peek secret database <span class="nt">-n</span> staging
</code></pre></div></div>

<p>Cargar reglas personalizadas para descubrir relaciones con CRDs:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl-peek secret   <span class="nt">--rules</span> ./examples/rules-all.yaml
</code></pre></div></div>

<p>La inspección de Secrets sigue incluyendo:</p>

<ul>
  <li>selección interactiva</li>
  <li>filtrado y paginación</li>
  <li>valores decodificados</li>
  <li>descubrimiento de relaciones integrado</li>
  <li>descubrimiento de CRDs mediante reglas YAML</li>
  <li>relaciones <code class="language-plaintext highlighter-rouge">uses</code>, <code class="language-plaintext highlighter-rouge">produces</code> y <code class="language-plaintext highlighter-rouge">references</code></li>
</ul>

<p>La diferencia es que ahora la CLI presenta la inspección de Secrets, la gestión de namespaces y las shells aisladas como flujos igualmente importantes.</p>

<hr />

<h2 id="por-qué-utilizar-kubeconfigs-temporales">¿Por qué utilizar kubeconfigs temporales?</h2>

<p>Configurar solamente una variable de entorno para el namespace no sería suficiente.</p>

<p>Las herramientas de Kubernetes no utilizan siempre los mismos flags ni variables, pero sí entienden el formato kubeconfig.</p>

<p>Al crear un kubeconfig temporal y exponerlo mediante <code class="language-plaintext highlighter-rouge">KUBECONFIG</code>, el alcance aislado funciona de forma natural con herramientas como:</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">kubectl</code></li>
  <li>Helm</li>
  <li>Flux</li>
  <li>flujos de Kustomize que ejecutan clientes de Kubernetes</li>
  <li>aplicaciones Go que utilizan <code class="language-plaintext highlighter-rouge">client-go</code></li>
  <li>otras herramientas compatibles con kubeconfig</li>
</ul>

<p>Esto hace que la shell sea útil más allá de un único comando de <code class="language-plaintext highlighter-rouge">kubectl</code>.</p>

<hr />

<h2 id="instalación">Instalación</h2>

<h3 id="homebrew">Homebrew</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>brew tap pierinho13/tools
brew <span class="nb">install</span> <span class="nt">--cask</span> kubectl-peek
</code></pre></div></div>

<p>Para actualizar una instalación existente:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>brew update
brew upgrade <span class="nt">--cask</span> kubectl-peek
</code></pre></div></div>

<p>Comprueba la versión instalada:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl-peek <span class="nt">--version</span>
</code></pre></div></div>

<hr />

<h3 id="github-releases">GitHub Releases</h3>

<p>En GitHub Releases se publican binarios precompilados para macOS, Linux y Windows.</p>

<hr />

<h3 id="compilar-desde-el-código-fuente">Compilar desde el código fuente</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git clone https://github.com/pierinho13/kubectl-peek.git
<span class="nb">cd </span>kubectl-peek
go build <span class="nt">-o</span> kubectl-peek <span class="nb">.</span>
</code></pre></div></div>

<hr />

<h2 id="ejemplos-de-uso">Ejemplos de uso</h2>

<h3 id="seleccionar-todo-de-forma-interactiva">Seleccionar todo de forma interactiva</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl-peek shell
</code></pre></div></div>

<h3 id="seleccionar-solamente-el-namespace">Seleccionar solamente el namespace</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl-peek shell <span class="nt">--context</span> production
</code></pre></div></div>

<h3 id="abrir-directamente-una-shell">Abrir directamente una shell</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl-peek shell <span class="nt">--context</span> production <span class="nt">-n</span> payments
</code></pre></div></div>

<h3 id="mantener-el-contexto-actual-y-seleccionar-un-namespace-temporal">Mantener el contexto actual y seleccionar un namespace temporal</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl-peek namespace <span class="nt">--shell</span>
</code></pre></div></div>

<h3 id="guardar-un-namespace-en-el-contexto-actual">Guardar un namespace en el contexto actual</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl-peek namespace
</code></pre></div></div>

<h3 id="inspeccionar-relaciones-de-un-secret">Inspeccionar relaciones de un Secret</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl-peek secret
</code></pre></div></div>

<hr />

<h2 id="reflexiones-finales">Reflexiones finales</h2>

<p>El objetivo de <code class="language-plaintext highlighter-rouge">kubectl-peek</code> no es sustituir a <code class="language-plaintext highlighter-rouge">kubectl</code>.</p>

<p>Su objetivo es eliminar fricción de varios flujos de Kubernetes que los platform engineers, SREs y desarrolladores repiten constantemente.</p>

<p>La inspección de Secrets ayuda a responder:</p>

<blockquote>
  <p>¿Con qué recursos está relacionado este Secret?</p>
</blockquote>

<p>El nuevo flujo de shells aisladas ayuda a responder:</p>

<blockquote>
  <p>¿Cómo puedo trabajar de forma segura en este contexto y namespace sin modificar mi entorno real?</p>
</blockquote>

<p>Esto convierte a <code class="language-plaintext highlighter-rouge">kubectl-peek</code> en una herramienta útil no solo para investigar Secrets, sino también durante despliegues, resolución de incidencias, comparación de clusters, diagnóstico de namespaces y trabajo diario con varios entornos.</p>

<p>El proyecto es open source:</p>

<p><strong>https://github.com/pierinho13/kubectl-peek</strong></p>

<p>Las contribuciones, sugerencias e ideas son bienvenidas.</p>

<p>Si te interesa este tipo de trabajo de platform engineering, puedes conocer mejor mi experiencia o <a href="/es/contact">contactar conmigo</a>.</p>]]></content><author><name></name></author><summary type="html"><![CDATA[kubectl-peek ahora permite abrir shells aisladas y conscientes del contexto mediante kubeconfigs temporales, para trabajar entre clusters y namespaces sin cambiar el contexto real ni repetir flags constantemente.]]></summary><media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="https://github.com/user-attachments/assets/9eb0f303-cc02-4cc2-b434-ead0ceb10c35" /><media:content medium="image" url="https://github.com/user-attachments/assets/9eb0f303-cc02-4cc2-b434-ead0ceb10c35" xmlns:media="http://search.yahoo.com/mrss/" /></entry><entry xml:lang="es"><title type="html">kubectl-peek: entiende las relaciones de los Secrets de Kubernetes desde la terminal</title><link href="http://localhost:4000/2026/07/17/kubectl-peek-es/" rel="alternate" type="text/html" title="kubectl-peek: entiende las relaciones de los Secrets de Kubernetes desde la terminal" /><published>2026-07-17T00:00:00+02:00</published><updated>2026-07-17T00:00:00+02:00</updated><id>http://localhost:4000/2026/07/17/kubectl-peek-es</id><content type="html" xml:base="http://localhost:4000/2026/07/17/kubectl-peek-es/"><![CDATA[<p>Trabajar con Secrets de Kubernetes es sencillo hasta que necesitas responder preguntas como:</p>

<ul>
  <li><strong>¿Qué contiene realmente este Secret?</strong></li>
  <li><strong>¿Qué workloads lo están utilizando?</strong></li>
  <li><strong>¿Ha sido generado por algún operador?</strong></li>
  <li><strong>¿Puedo eliminarlo sin romper nada?</strong></li>
</ul>

<p>Responder a estas preguntas suele implicar varios comandos de <code class="language-plaintext highlighter-rouge">kubectl</code>, decodificar valores en Base64, buscar entre manifiestos y revisar recursos personalizados uno por uno.</p>

<p>Quería una forma mucho más sencilla de hacerlo.</p>

<p>Por eso nació <strong>kubectl-peek</strong>.</p>

<p><code class="language-plaintext highlighter-rouge">kubectl-peek</code> es una CLI interactiva y un plugin nativo de <code class="language-plaintext highlighter-rouge">kubectl</code> centrado en inspeccionar Secrets de Kubernetes y entender sus relaciones directamente desde la terminal.</p>

<p>Sin controladores.</p>

<p>Sin CRDs instalados por la herramienta.</p>

<p>Sin interfaz web.</p>

<p>Sin componentes dentro del clúster.</p>

<p>Todo funciona en local utilizando el <code class="language-plaintext highlighter-rouge">kubeconfig</code> y los permisos RBAC que ya tienes.</p>

<hr />

<h2 id="por-qué-lo-he-creado">¿Por qué lo he creado?</h2>

<p>Mi objetivo era desarrollar una herramienta deliberadamente pequeña, pero realmente útil para el trabajo diario de Platform Engineering.</p>

<p>Quería algo que:</p>

<ul>
  <li>funcionase con cualquier clúster de Kubernetes</li>
  <li>no necesitase instalación dentro del clúster</li>
  <li>respetase el kubeconfig y el RBAC existentes</li>
  <li>mantuviese la inspección de Secrets rápida e interactiva</li>
  <li>evitase llamadas adicionales a la API cuando no se necesita descubrir relaciones</li>
</ul>

<p>En lugar de copiar nombres de Secrets entre comandos, decodificar valores manualmente, abrir manifiestos de workloads y buscar referencias entre operadores o CRDs, quería reunirlo todo en un único flujo.</p>

<hr />

<h2 id="qué-hace-kubectl-peek">¿Qué hace kubectl-peek?</h2>

<p>La herramienta proporciona un explorador interactivo de Secrets directamente desde la terminal.</p>

<p>Permite:</p>

<ul>
  <li>explorar Secrets de un namespace</li>
  <li>filtrar la lista de forma interactiva</li>
  <li>inspeccionar valores decodificados</li>
  <li>ocultar los valores cuando no deben mostrarse</li>
  <li>descubrir recursos que utilizan un Secret</li>
  <li>detectar recursos que producen o referencian un Secret</li>
  <li>ampliar el descubrimiento a recursos personalizados mediante reglas YAML</li>
  <li>utilizarlo tanto como <code class="language-plaintext highlighter-rouge">kubectl-peek</code> como mediante <code class="language-plaintext highlighter-rouge">kubectl peek</code></li>
</ul>

<p>Por defecto, los valores del Secret se muestran y el descubrimiento de relaciones está desactivado.</p>

<p>Esto mantiene la inspección habitual rápida y evita llamadas innecesarias a la API o avisos de permisos cuando solo necesitas consultar un Secret.</p>

<hr />

<h2 id="demo">Demo</h2>

<p><img width="1200" height="700" alt="Demo de inspección de Secrets con kubectl-peek" src="https://github.com/user-attachments/assets/c6c5b2ac-0ea4-4774-8315-12021e88e829" /></p>

<p>El flujo básico se mantiene deliberadamente sencillo:</p>

<ol>
  <li>Ejecutar <code class="language-plaintext highlighter-rouge">kubectl-peek secret</code></li>
  <li>Filtrar la lista</li>
  <li>Seleccionar un Secret</li>
  <li>Revisar sus metadatos</li>
  <li>Consultar sus valores decodificados u ocultos</li>
  <li>Activar el descubrimiento de relaciones solo cuando sea necesario</li>
</ol>

<hr />

<h2 id="inspeccionar-los-valores-de-un-secret">Inspeccionar los valores de un Secret</h2>

<p>Inicia el explorador interactivo con:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl-peek secret
</code></pre></div></div>

<p>o mediante el plugin nativo:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl peek secret
</code></pre></div></div>

<p>Filtra por nombre:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl-peek secret database
</code></pre></div></div>

<p>Utiliza otro namespace:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl-peek secret database <span class="nt">-n</span> staging
</code></pre></div></div>

<p>Por defecto, los valores decodificados se muestran directamente en la terminal.</p>

<p>Ejemplo:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Secret: database-credentials
Namespace: staging
Type: Opaque

password:
────────────────────────────────────────────────────────────
example-password
</code></pre></div></div>

<hr />

<h2 id="ocultar-los-valores-del-secret">Ocultar los valores del Secret</h2>

<p>En algunos casos necesitas consultar los metadatos y las relaciones, pero no debes mostrar los valores reales.</p>

<p>Utiliza:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl-peek secret <span class="nt">--show-values</span><span class="o">=</span><span class="nb">false</span>
</code></pre></div></div>

<p>El nombre de la clave continúa visible y el valor se sustituye por su tamaño en bytes:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>password:
────────────────────────────────────────────────────────────
&lt;redacted: 24 bytes&gt;
</code></pre></div></div>

<p>Esto resulta útil durante:</p>

<ul>
  <li>sesiones de pantalla compartida</li>
  <li>grabaciones de terminal</li>
  <li>capturas</li>
  <li>demostraciones</li>
  <li>troubleshooting colaborativo</li>
  <li>entornos donde el contenido del Secret debe permanecer oculto</li>
</ul>

<hr />

<h2 id="el-descubrimiento-de-relaciones-es-opcional">El descubrimiento de relaciones es opcional</h2>

<p>El descubrimiento de relaciones está desactivado por defecto.</p>

<p>Actívalo explícitamente con:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl-peek secret <span class="nt">--show-usage</span>
</code></pre></div></div>

<p>Cuando está activado, <code class="language-plaintext highlighter-rouge">kubectl-peek</code> busca relaciones en recursos soportados de Kubernetes y en las reglas configuradas para recursos personalizados.</p>

<p>Las relaciones pueden ser:</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">uses</code></li>
  <li><code class="language-plaintext highlighter-rouge">produces</code></li>
  <li><code class="language-plaintext highlighter-rouge">references</code></li>
</ul>

<p>Ejemplo:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Secret: database-credentials
Namespace: staging
Type: Opaque
Used by:
  Deployment/backend
    uses: container environment (container/backend envFrom)
  CronJob/backup
    uses: environment variable (container/backup env/BACKUP_PASSWORD -&gt; password)
</code></pre></div></div>

<p>También puedes combinar el descubrimiento con la ocultación de valores:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl-peek secret <span class="se">\</span>
  <span class="nt">--show-usage</span> <span class="se">\</span>
  <span class="nt">--show-values</span><span class="o">=</span><span class="nb">false</span>
</code></pre></div></div>

<p>Así puedes consultar las dependencias sin imprimir información sensible.</p>

<hr />

<h2 id="descubrimiento-integrado-de-secrets">Descubrimiento integrado de Secrets</h2>

<p>Con <code class="language-plaintext highlighter-rouge">--show-usage</code>, <code class="language-plaintext highlighter-rouge">kubectl-peek</code> puede descubrir referencias desde recursos habituales de Kubernetes.</p>

<p>Actualmente incluye recursos como:</p>

<ul>
  <li>Pods</li>
  <li>Deployments</li>
  <li>StatefulSets</li>
  <li>DaemonSets</li>
  <li>Jobs</li>
  <li>CronJobs</li>
  <li>ServiceAccounts</li>
  <li>Ingresses</li>
  <li>Gateways de Gateway API</li>
</ul>

<p>Detecta referencias mediante campos como:</p>

<ul>
  <li>volúmenes basados en Secrets</li>
  <li>projected Secret volumes</li>
  <li><code class="language-plaintext highlighter-rouge">imagePullSecrets</code></li>
  <li>variables de entorno</li>
  <li><code class="language-plaintext highlighter-rouge">envFrom</code></li>
  <li>init containers</li>
  <li>ephemeral containers</li>
  <li>certificados de listeners de Gateway API</li>
  <li>configuración TLS de Ingress</li>
</ul>

<p>Esto facilita comprobar si un Secret sigue estando referenciado antes de rotarlo o eliminarlo.</p>

<hr />

<h2 id="un-mensaje-más-seguro-cuando-no-se-encuentra-nada">Un mensaje más seguro cuando no se encuentra nada</h2>

<p>Que no se encuentre una relación no significa que el Secret esté definitivamente sin uso.</p>

<p>Cuando <code class="language-plaintext highlighter-rouge">--show-usage</code> está activado pero no se detecta ninguna referencia soportada, <code class="language-plaintext highlighter-rouge">kubectl-peek</code> muestra:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Used by:
  No references were found among the supported built-in resources and configured usage rules.
  This does not guarantee that the Secret is unused; unsupported resources, external systems, or unconfigured custom resources may still reference it.
</code></pre></div></div>

<p>Esta distinción es importante porque el Secret todavía podría ser utilizado por:</p>

<ul>
  <li>un recurso de Kubernetes aún no soportado</li>
  <li>un recurso personalizado sin una regla configurada</li>
  <li>un sistema externo</li>
  <li>una aplicación que obtiene el Secret dinámicamente</li>
  <li>un recurso ubicado en otro namespace</li>
</ul>

<p>La salida aporta información sobre dependencias, pero no garantiza que eliminar el Secret sea seguro.</p>

<hr />

<h2 id="descubrir-relaciones-en-recursos-personalizados">Descubrir relaciones en recursos personalizados</h2>

<p>En lugar de incorporar soporte específico para cada operador de Kubernetes, <code class="language-plaintext highlighter-rouge">kubectl-peek</code> puede cargar reglas de relaciones desde un fichero YAML.</p>

<p><img width="1200" height="700" alt="Demo del descubrimiento de relaciones de Secrets con kubectl-peek" src="https://github.com/user-attachments/assets/cc221426-06b2-4c7d-ab71-934b6b6e623f" /></p>

<p>Esto permite añadir soporte para recursos como:</p>

<ul>
  <li>External Secrets Operator</li>
  <li>cert-manager</li>
  <li>Crossplane</li>
  <li>operadores de Vault</li>
  <li>APIs internas de plataforma</li>
  <li>CRDs privados</li>
</ul>

<p>sin modificar la aplicación.</p>

<p>Ejemplo:</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">rules</span><span class="pi">:</span>
  <span class="pi">-</span> <span class="na">apiVersions</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s">external-secrets.io/v1</span>
      <span class="pi">-</span> <span class="s">external-secrets.io/v1beta1</span>
    <span class="na">kind</span><span class="pi">:</span> <span class="s">ExternalSecret</span>
    <span class="na">resource</span><span class="pi">:</span> <span class="s">externalsecrets</span>
    <span class="na">references</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="na">path</span><span class="pi">:</span> <span class="s">spec.target.name</span>
        <span class="na">relation</span><span class="pi">:</span> <span class="s">produces</span>
        <span class="na">description</span><span class="pi">:</span> <span class="s">generated Secret</span>
</code></pre></div></div>

<p>Carga el fichero de reglas explícitamente:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl-peek secret <span class="se">\</span>
  <span class="nt">--show-usage</span> <span class="se">\</span>
  <span class="nt">--rules</span> rules.yaml
</code></pre></div></div>

<p>O configura una ruta por defecto:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">export </span><span class="nv">KUBECTL_PEEK_RULE_FILE</span><span class="o">=</span><span class="s2">"</span><span class="nv">$HOME</span><span class="s2">/.config/kubectl-peek/rules.yaml"</span>
</code></pre></div></div>

<p>Después ejecuta:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl-peek secret <span class="nt">--show-usage</span>
</code></pre></div></div>

<p>Las relaciones integradas y las definidas mediante reglas YAML se combinan en la misma sección <code class="language-plaintext highlighter-rouge">Used by</code>.</p>

<hr />

<h2 id="instalación">Instalación</h2>

<h3 id="homebrew">Homebrew</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>brew tap pierinho13/tools
brew <span class="nb">install</span> <span class="nt">--cask</span> kubectl-peek
</code></pre></div></div>

<h3 id="github-releases">GitHub Releases</h3>

<p>Descarga el archivo correspondiente a tu sistema operativo y arquitectura, extráelo y coloca el binario en un directorio incluido en tu <code class="language-plaintext highlighter-rouge">PATH</code>.</p>

<h3 id="compilar-desde-el-código-fuente">Compilar desde el código fuente</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git clone https://github.com/pierinho13/kubectl-peek.git
<span class="nb">cd </span>kubectl-peek
go build <span class="nt">-o</span> kubectl-peek <span class="nb">.</span>
</code></pre></div></div>

<hr />

<h2 id="uso-básico">Uso básico</h2>

<p>Explorar Secrets:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl-peek secret
</code></pre></div></div>

<p>Filtrar por nombre:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl-peek secret database
</code></pre></div></div>

<p>Utilizar otro namespace:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl-peek secret <span class="nt">-n</span> staging
</code></pre></div></div>

<p>Utilizar otro contexto:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl-peek secret <span class="nt">--context</span> production
</code></pre></div></div>

<p>Especificar otro kubeconfig:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl-peek secret <span class="nt">--kubeconfig</span> ~/.kube/config
</code></pre></div></div>

<p>Descubrir relaciones:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl-peek secret <span class="nt">--show-usage</span>
</code></pre></div></div>

<p>Ocultar valores:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl-peek secret <span class="nt">--show-values</span><span class="o">=</span><span class="nb">false</span>
</code></pre></div></div>

<p>Cargar reglas personalizadas:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl-peek secret <span class="se">\</span>
  <span class="nt">--show-usage</span> <span class="se">\</span>
  <span class="nt">--rules</span> rules.yaml
</code></pre></div></div>

<hr />

<h2 id="permisos">Permisos</h2>

<p>La inspección básica requiere permisos para leer Secrets en el namespace seleccionado.</p>

<p>El descubrimiento de relaciones necesita permisos adicionales de tipo <code class="language-plaintext highlighter-rouge">list</code> sobre los recursos soportados o configurados que se quieran inspeccionar.</p>

<p>Esto permite que:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl-peek secret
</code></pre></div></div>

<p>siga siendo útil con un conjunto de permisos más reducido, mientras que:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl-peek secret <span class="nt">--show-usage</span>
</code></pre></div></div>

<p>realiza la búsqueda más amplia únicamente cuando se solicita.</p>

<hr />

<h2 id="seguridad">Seguridad</h2>

<p>Los valores decodificados se muestran por defecto.</p>

<p>Esos valores pueden permanecer visibles en:</p>

<ul>
  <li>el scrollback de la terminal</li>
  <li>grabaciones de pantalla</li>
  <li>capturas</li>
  <li>sesiones compartidas</li>
  <li>salidas de comandos capturadas</li>
</ul>

<p>Utiliza:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl-peek secret <span class="nt">--show-values</span><span class="o">=</span><span class="nb">false</span>
</code></pre></div></div>

<p>cuando los valores deban permanecer ocultos.</p>

<p>La herramienta debe seguir utilizándose únicamente en entornos de confianza y con los permisos RBAC mínimos necesarios para cada tarea.</p>

<hr />

<h2 id="conclusión">Conclusión</h2>

<p><code class="language-plaintext highlighter-rouge">kubectl-peek</code> comenzó como una pequeña utilidad para inspeccionar Secrets de Kubernetes de forma más cómoda.</p>

<p>Su objetivo es simplificar dos flujos relacionados:</p>

<blockquote>
  <p><strong>Inspeccionar qué contiene un Secret.</strong></p>
</blockquote>

<p>y:</p>

<blockquote>
  <p><strong>Entender qué recursos soportados lo utilizan, producen o referencian.</strong></p>
</blockquote>

<p>El modelo de relaciones es deliberadamente transparente. Los detectores integrados cubren recursos habituales de Kubernetes, las reglas YAML amplían el mismo mecanismo a recursos personalizados y una búsqueda vacía se presenta claramente como información incompleta, no como una garantía de que el Secret pueda eliminarse de forma segura.</p>

<p>El proyecto es open source:</p>

<p><strong>https://github.com/pierinho13/kubectl-peek</strong></p>

<p>Las contribuciones, ideas y nuevas reglas de ejemplo son bienvenidas.</p>

<p>Si te interesa este tipo de trabajo de Platform Engineering, puedes conocer más sobre mi experiencia o <a href="/contact">ponerte en contacto</a>.</p>]]></content><author><name></name></author><summary type="html"><![CDATA[kubectl-peek es un plugin interactivo para kubectl que permite inspeccionar Secrets de Kubernetes, ocultar sus valores cuando sea necesario y descubrir los recursos que los usan, producen o referencian.]]></summary><media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="https://github.com/user-attachments/assets/c6c5b2ac-0ea4-4774-8315-12021e88e829" /><media:content medium="image" url="https://github.com/user-attachments/assets/c6c5b2ac-0ea4-4774-8315-12021e88e829" xmlns:media="http://search.yahoo.com/mrss/" /></entry><entry xml:lang="en"><title type="html">kubectl-peek: understand Kubernetes Secret relationships from your terminal</title><link href="http://localhost:4000/2026/07/17/kubectl-peek/" rel="alternate" type="text/html" title="kubectl-peek: understand Kubernetes Secret relationships from your terminal" /><published>2026-07-17T00:00:00+02:00</published><updated>2026-07-17T00:00:00+02:00</updated><id>http://localhost:4000/2026/07/17/kubectl-peek</id><content type="html" xml:base="http://localhost:4000/2026/07/17/kubectl-peek/"><![CDATA[<p>Working with Kubernetes Secrets is easy until you need to answer questions such as:</p>

<ul>
  <li><strong>What does this Secret contain?</strong></li>
  <li><strong>Which workloads use it?</strong></li>
  <li><strong>Was it generated by an operator?</strong></li>
  <li><strong>Is it safe to delete?</strong></li>
</ul>

<p>Answering those questions usually means switching between several <code class="language-plaintext highlighter-rouge">kubectl</code> commands, decoding Base64 values manually, grepping manifests, and inspecting custom resources one by one.</p>

<p>I wanted something much simpler.</p>

<p>That is why I built <strong>kubectl-peek</strong>.</p>

<p><code class="language-plaintext highlighter-rouge">kubectl-peek</code> is a lightweight interactive CLI and native <code class="language-plaintext highlighter-rouge">kubectl</code> plugin focused on inspecting Kubernetes Secrets and understanding their relationships from the terminal.</p>

<p>No controllers.</p>

<p>No CRDs installed by the tool.</p>

<p>No web UI.</p>

<p>No cluster-side components.</p>

<p>Everything runs client-side using the kubeconfig and RBAC permissions you already have.</p>

<hr />

<h2 id="why-i-built-it">Why I built it</h2>

<p>My goal was to create something intentionally small but genuinely useful during day-to-day platform engineering work.</p>

<p>I wanted a tool that:</p>

<ul>
  <li>works with any Kubernetes cluster</li>
  <li>requires no cluster-side installation</li>
  <li>respects the existing kubeconfig and RBAC model</li>
  <li>keeps Secret inspection fast and interactive</li>
  <li>avoids unnecessary API calls unless relationship discovery is requested</li>
</ul>

<p>Instead of copying Secret names between commands, decoding values manually, opening workload manifests, and hunting through operators or CRDs, I wanted one focused workflow.</p>

<hr />

<h2 id="what-kubectl-peek-does">What kubectl-peek does</h2>

<p>The tool provides an interactive Secret browser directly in your terminal.</p>

<p>You can:</p>

<ul>
  <li>browse Secrets in a namespace</li>
  <li>filter the list interactively</li>
  <li>inspect decoded Secret values</li>
  <li>redact values when they should not be displayed</li>
  <li>discover resources that use a Secret</li>
  <li>detect resources that produce or reference a Secret</li>
  <li>extend relationship discovery to custom resources through YAML rules</li>
  <li>use it as either <code class="language-plaintext highlighter-rouge">kubectl-peek</code> or <code class="language-plaintext highlighter-rouge">kubectl peek</code></li>
</ul>

<p>By default, Secret values are displayed and relationship discovery is disabled.</p>

<p>This keeps normal inspection fast and avoids extra API requests or permission warnings when you only need to inspect a Secret.</p>

<hr />

<h2 id="demo">Demo</h2>

<p><img width="1200" height="700" alt="kubectl-peek Secret inspection demo" src="https://github.com/user-attachments/assets/c6c5b2ac-0ea4-4774-8315-12021e88e829" /></p>

<p>The basic workflow stays intentionally simple:</p>

<ol>
  <li>Run <code class="language-plaintext highlighter-rouge">kubectl-peek secret</code></li>
  <li>Filter the Secret list</li>
  <li>Select a Secret</li>
  <li>Inspect its metadata</li>
  <li>Review decoded or redacted values</li>
  <li>Enable relationship discovery only when needed</li>
</ol>

<hr />

<h2 id="inspect-secret-values">Inspect Secret values</h2>

<p>Start the interactive browser with:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl-peek secret
</code></pre></div></div>

<p>or through the native plugin form:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl peek secret
</code></pre></div></div>

<p>Filter by name:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl-peek secret database
</code></pre></div></div>

<p>Use another namespace:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl-peek secret database <span class="nt">-n</span> staging
</code></pre></div></div>

<p>By default, decoded values are displayed directly in the terminal.</p>

<p>Example:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Secret: database-credentials
Namespace: staging
Type: Opaque

password:
────────────────────────────────────────────────────────────
example-password
</code></pre></div></div>

<hr />

<h2 id="hide-secret-values">Hide Secret values</h2>

<p>Sometimes you need the metadata and relationships but should not expose the actual values.</p>

<p>Use:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl-peek secret <span class="nt">--show-values</span><span class="o">=</span><span class="nb">false</span>
</code></pre></div></div>

<p>The key remains visible while the value is replaced with its byte length:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>password:
────────────────────────────────────────────────────────────
&lt;redacted: 24 bytes&gt;
</code></pre></div></div>

<p>This is useful during:</p>

<ul>
  <li>screen sharing</li>
  <li>terminal recordings</li>
  <li>screenshots</li>
  <li>demos</li>
  <li>collaborative troubleshooting</li>
  <li>environments where Secret contents should stay hidden</li>
</ul>

<hr />

<h2 id="relationship-discovery-is-opt-in">Relationship discovery is opt-in</h2>

<p>Relationship discovery is disabled by default.</p>

<p>Enable it explicitly with:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl-peek secret <span class="nt">--show-usage</span>
</code></pre></div></div>

<p>When enabled, <code class="language-plaintext highlighter-rouge">kubectl-peek</code> searches supported Kubernetes resources and configured custom-resource rules for relationships such as:</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">uses</code></li>
  <li><code class="language-plaintext highlighter-rouge">produces</code></li>
  <li><code class="language-plaintext highlighter-rouge">references</code></li>
</ul>

<p>Example:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Secret: database-credentials
Namespace: staging
Type: Opaque
Used by:
  Deployment/backend
    uses: container environment (container/backend envFrom)
  CronJob/backup
    uses: environment variable (container/backup env/BACKUP_PASSWORD -&gt; password)
</code></pre></div></div>

<p>You can combine discovery with value redaction:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl-peek secret <span class="se">\</span>
  <span class="nt">--show-usage</span> <span class="se">\</span>
  <span class="nt">--show-values</span><span class="o">=</span><span class="nb">false</span>
</code></pre></div></div>

<p>That gives you the dependency information without printing sensitive values.</p>

<hr />

<h2 id="built-in-secret-discovery">Built-in Secret discovery</h2>

<p>With <code class="language-plaintext highlighter-rouge">--show-usage</code>, <code class="language-plaintext highlighter-rouge">kubectl-peek</code> can discover references from common Kubernetes resources.</p>

<p>Currently supported resources include:</p>

<ul>
  <li>Pods</li>
  <li>Deployments</li>
  <li>StatefulSets</li>
  <li>DaemonSets</li>
  <li>Jobs</li>
  <li>CronJobs</li>
  <li>ServiceAccounts</li>
  <li>Ingresses</li>
  <li>Gateway API Gateways</li>
</ul>

<p>It detects references through fields such as:</p>

<ul>
  <li>Secret volumes</li>
  <li>projected Secret volumes</li>
  <li><code class="language-plaintext highlighter-rouge">imagePullSecrets</code></li>
  <li>environment variables</li>
  <li><code class="language-plaintext highlighter-rouge">envFrom</code></li>
  <li>init containers</li>
  <li>ephemeral containers</li>
  <li>Gateway listener certificates</li>
  <li>Ingress TLS configuration</li>
</ul>

<p>This makes it much easier to understand whether a Secret is still referenced before rotating or deleting it.</p>

<hr />

<h2 id="a-safer-message-when-nothing-is-found">A safer message when nothing is found</h2>

<p>A missing relationship must not be interpreted as proof that a Secret is unused.</p>

<p>When <code class="language-plaintext highlighter-rouge">--show-usage</code> is enabled but no supported reference is found, <code class="language-plaintext highlighter-rouge">kubectl-peek</code> displays:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Used by:
  No references were found among the supported built-in resources and configured usage rules.
  This does not guarantee that the Secret is unused; unsupported resources, external systems, or unconfigured custom resources may still reference it.
</code></pre></div></div>

<p>This distinction matters because a Secret may still be used by:</p>

<ul>
  <li>an unsupported Kubernetes resource</li>
  <li>a custom resource without a configured rule</li>
  <li>an external system</li>
  <li>an application that retrieves the Secret dynamically</li>
  <li>a resource in another namespace</li>
</ul>

<p>The output is dependency information, not a deletion guarantee.</p>

<hr />

<h2 id="discover-relationships-in-custom-resources">Discover relationships in custom resources</h2>

<p>Instead of hardcoding every Kubernetes operator, <code class="language-plaintext highlighter-rouge">kubectl-peek</code> can load relationship rules from a YAML file.</p>

<p><img width="1200" height="700" alt="kubectl-peek Secret relationship discovery demo" src="https://github.com/user-attachments/assets/cc221426-06b2-4c7d-ab71-934b6b6e623f" /></p>

<p>That makes it possible to support resources such as:</p>

<ul>
  <li>External Secrets Operator</li>
  <li>cert-manager</li>
  <li>Crossplane</li>
  <li>Vault operators</li>
  <li>internal platform APIs</li>
  <li>private CRDs</li>
</ul>

<p>without modifying the application itself.</p>

<p>Example:</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">rules</span><span class="pi">:</span>
  <span class="pi">-</span> <span class="na">apiVersions</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s">external-secrets.io/v1</span>
      <span class="pi">-</span> <span class="s">external-secrets.io/v1beta1</span>
    <span class="na">kind</span><span class="pi">:</span> <span class="s">ExternalSecret</span>
    <span class="na">resource</span><span class="pi">:</span> <span class="s">externalsecrets</span>
    <span class="na">references</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="na">path</span><span class="pi">:</span> <span class="s">spec.target.name</span>
        <span class="na">relation</span><span class="pi">:</span> <span class="s">produces</span>
        <span class="na">description</span><span class="pi">:</span> <span class="s">generated Secret</span>
</code></pre></div></div>

<p>Load the rule file explicitly:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl-peek secret <span class="se">\</span>
  <span class="nt">--show-usage</span> <span class="se">\</span>
  <span class="nt">--rules</span> rules.yaml
</code></pre></div></div>

<p>Or configure a persistent default:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">export </span><span class="nv">KUBECTL_PEEK_RULE_FILE</span><span class="o">=</span><span class="s2">"</span><span class="nv">$HOME</span><span class="s2">/.config/kubectl-peek/rules.yaml"</span>
</code></pre></div></div>

<p>Then run:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl-peek secret <span class="nt">--show-usage</span>
</code></pre></div></div>

<p>Built-in and rule-based relationships are combined in the same <code class="language-plaintext highlighter-rouge">Used by</code> section.</p>

<hr />

<h2 id="installation">Installation</h2>

<h3 id="homebrew">Homebrew</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>brew tap pierinho13/tools
brew <span class="nb">install</span> <span class="nt">--cask</span> kubectl-peek
</code></pre></div></div>

<h3 id="github-releases">GitHub Releases</h3>

<p>Download the archive for your operating system and architecture, extract it, and place the binary in a directory included in your <code class="language-plaintext highlighter-rouge">PATH</code>.</p>

<h3 id="build-from-source">Build from source</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git clone https://github.com/pierinho13/kubectl-peek.git
<span class="nb">cd </span>kubectl-peek
go build <span class="nt">-o</span> kubectl-peek <span class="nb">.</span>
</code></pre></div></div>

<hr />

<h2 id="basic-usage">Basic usage</h2>

<p>Browse Secrets:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl-peek secret
</code></pre></div></div>

<p>Filter by name:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl-peek secret database
</code></pre></div></div>

<p>Use another namespace:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl-peek secret <span class="nt">-n</span> staging
</code></pre></div></div>

<p>Use another context:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl-peek secret <span class="nt">--context</span> production
</code></pre></div></div>

<p>Specify a kubeconfig:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl-peek secret <span class="nt">--kubeconfig</span> ~/.kube/config
</code></pre></div></div>

<p>Discover relationships:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl-peek secret <span class="nt">--show-usage</span>
</code></pre></div></div>

<p>Hide values:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl-peek secret <span class="nt">--show-values</span><span class="o">=</span><span class="nb">false</span>
</code></pre></div></div>

<p>Load custom discovery rules:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl-peek secret <span class="se">\</span>
  <span class="nt">--show-usage</span> <span class="se">\</span>
  <span class="nt">--rules</span> rules.yaml
</code></pre></div></div>

<hr />

<h2 id="permissions">Permissions</h2>

<p>Basic Secret inspection requires permission to read Secrets in the selected namespace.</p>

<p>Relationship discovery requires additional <code class="language-plaintext highlighter-rouge">list</code> permissions for the supported or configured resources being inspected.</p>

<p>This means:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl-peek secret
</code></pre></div></div>

<p>can remain useful with a smaller permission set, while:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl-peek secret <span class="nt">--show-usage</span>
</code></pre></div></div>

<p>performs the broader dependency search only when requested.</p>

<hr />

<h2 id="security">Security</h2>

<p>Decoded Secret values are displayed by default.</p>

<p>Those values may remain visible in:</p>

<ul>
  <li>terminal scrollback</li>
  <li>screen recordings</li>
  <li>screenshots</li>
  <li>shared sessions</li>
  <li>captured command output</li>
</ul>

<p>Use:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl-peek secret <span class="nt">--show-values</span><span class="o">=</span><span class="nb">false</span>
</code></pre></div></div>

<p>when values should remain hidden.</p>

<p>The tool should still be used only in trusted environments and with the minimum RBAC permissions required for the task.</p>

<hr />

<h2 id="final-thoughts">Final thoughts</h2>

<p><code class="language-plaintext highlighter-rouge">kubectl-peek</code> started as a small utility for inspecting Kubernetes Secrets more comfortably.</p>

<p>Its purpose is to make two related workflows easier:</p>

<blockquote>
  <p><strong>Inspect what a Secret contains.</strong></p>
</blockquote>

<p>and:</p>

<blockquote>
  <p><strong>Understand which supported resources use, produce, or reference it.</strong></p>
</blockquote>

<p>The relationship model is intentionally transparent. Built-in finders cover common Kubernetes resources, YAML rules extend the same mechanism to custom resources, and an empty result is clearly presented as incomplete evidence rather than proof that a Secret is safe to delete.</p>

<p>The project is open source:</p>

<p><strong>https://github.com/pierinho13/kubectl-peek</strong></p>

<p>Contributions, ideas, and new rule examples are welcome.</p>

<p>If you’re interested in this kind of platform engineering work, you can learn more about my experience or <a href="/en/contact">get in touch</a>.</p>]]></content><author><name></name></author><summary type="html"><![CDATA[kubectl-peek is an interactive kubectl plugin for inspecting Kubernetes Secrets, optionally hiding their values, and discovering the resources that use, produce, or reference them.]]></summary><media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="https://github.com/user-attachments/assets/c6c5b2ac-0ea4-4774-8315-12021e88e829" /><media:content medium="image" url="https://github.com/user-attachments/assets/c6c5b2ac-0ea4-4774-8315-12021e88e829" xmlns:media="http://search.yahoo.com/mrss/" /></entry><entry xml:lang="es"><title type="html">Cuando Elastic APM funciona… pero no ves ningún error</title><link href="http://localhost:4000/2026/01/29/apm-funciona-pero-no-ves-errores-es/" rel="alternate" type="text/html" title="Cuando Elastic APM funciona… pero no ves ningún error" /><published>2026-01-29T00:00:00+01:00</published><updated>2026-01-29T00:00:00+01:00</updated><id>http://localhost:4000/2026/01/29/apm-funciona-pero-no-ves-errores-es</id><content type="html" xml:base="http://localhost:4000/2026/01/29/apm-funciona-pero-no-ves-errores-es/"><![CDATA[<p>Elastic APM estaba funcionando.
Las transacciones aparecían.
Las llamadas entre servicios se veían con bastante claridad.</p>

<p>Y aun así, había algo profundamente frustrante:
los errores no aparecían.</p>

<p>La aplicación tenía excepciones reales, visibles en los logs, pero en la UI de APM:</p>

<ul>
  <li>No había errores.</li>
  <li>No había stacktraces.</li>
  <li>Las trazas parecían “limpias”.</li>
</ul>

<p>El problema no era evidente. Y tampoco estaba donde yo pensaba.</p>

<h2 id="el-contexto">El contexto</h2>

<p>Estaba integrando Elastic APM en una aplicación Java sobre Tomcat.</p>

<p>La integración básica era correcta:</p>

<ul>
  <li>El agente APM arrancaba sin errores.</li>
  <li>APM mostraba servicios y transacciones.</li>
  <li>Las peticiones se trazaban correctamente.</li>
</ul>

<p>A nivel superficial, APM funcionaba.
A nivel diagnóstico, no.</p>

<h2 id="el-síntoma-real-trazas-sin-contexto-de-error">El síntoma real: trazas sin contexto de error</h2>

<p>Las trazas mostraban:</p>

<ul>
  <li>Duraciones.</li>
  <li>Llamadas.</li>
  <li>El flujo general de la petición.</li>
</ul>

<p>Pero faltaba lo más importante cuando algo fallaba:</p>

<ul>
  <li>Ningún error asociado.</li>
  <li>Ningún stacktrace.</li>
  <li>Ninguna pista clara del motivo real del fallo.</li>
</ul>

<p>Esto es especialmente engañoso porque APM no estaba vacío.
Estaba incompleto.</p>

<p><img src="/assets/img/elastic-image-1.png" alt="Elastic APM sin errores" style="max-width: 600px; margin: 20px 0;" /></p>

<h2 id="primer-impulso-añadir-más-instrumentación">Primer impulso: añadir más instrumentación</h2>

<p>Mi primera reacción fue pensar que el problema era de detalle insuficiente en las trazas.</p>

<p>Probé inicialmente a forzar instrumentación con:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ELASTIC_APM_TRACE_METHODS=com.pierinho13.*#*
</code></pre></div></div>

<p>El resultado fue inmediato, pero poco útil a largo plazo:</p>

<ul>
  <li>Muchísimo más detalle.</li>
  <li>Demasiados métodos instrumentados.</li>
  <li>Un volumen de spans difícil de manejar.</li>
  <li>Ruido excesivo para un entorno real.</li>
</ul>

<p>APM dejaba de ser una herramienta de diagnóstico y pasaba a ser un volcán de datos.</p>

<h2 id="un-mejor-equilibrio-inferred-spans-por-profiling">Un mejor equilibrio: inferred spans por profiling</h2>

<p>Buscando una alternativa más razonable, activé inferred spans mediante profiling:</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">ELASTIC_APM_PROFILING_INFERRED_SPANS_ENABLED</span>
  <span class="na">value</span><span class="pi">:</span> <span class="s2">"</span><span class="s">true"</span>

<span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">ELASTIC_APM_PROFILING_INFERRED_SPANS_MIN_DURATION</span>
  <span class="na">value</span><span class="pi">:</span> <span class="s2">"</span><span class="s">10ms"</span>

<span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">ELASTIC_APM_PROFILING_INFERRED_SPANS_SAMPLING_INTERVAL</span>
  <span class="na">value</span><span class="pi">:</span> <span class="s2">"</span><span class="s">50ms"</span>
</code></pre></div></div>

<p>Aquí hubo un cambio claro:</p>

<ul>
  <li>Las trazas empezaron a mostrar mucho más detalle interno.</li>
  <li>El recorrido real de la petición se entendía mejor.</li>
  <li>Sin el coste ni la verbosidad de TRACE_METHODS.</li>
</ul>

<p>APM empezó a ser realmente útil.</p>

<p>Pero los errores seguían sin aparecer.</p>

<p><img src="/assets/img/elastic-image-2.png" alt="Inferred spans con profiling" style="max-width: 600px; margin: 20px 0;" /></p>

<h2 id="el-error-de-enfoque">El error de enfoque</h2>

<p>En ese punto estaba claro que:</p>

<ul>
  <li>El agente funcionaba.</li>
  <li>La instrumentación funcionaba.</li>
  <li>Las trazas se generaban correctamente.</li>
</ul>

<p>El problema no estaba en cómo se capturaban los datos, sino en qué pasaba con ellos después.</p>

<h2 id="el-verdadero-causante-templates-e-ilm-pisando-apm">El verdadero causante: templates e ILM pisando APM</h2>

<p>La causa real no estaba en APM, sino en la gestión de logs.</p>

<p>Había un <strong>template genérico para <code class="language-plaintext highlighter-rouge">logs-*-*</code></strong> con una prioridad más alta que los templates específicos de APM.
Eso hacía que, para data streams como <code class="language-plaintext highlighter-rouge">logs-apm.error-*</code>, ganara el template genérico en lugar del que Fleet y APM esperan.</p>

<p>Además, las operaciones de ILM se aplicaban de forma indiscriminada a todos los data streams bajo <code class="language-plaintext highlighter-rouge">logs-*</code>, incluyendo los de APM, que tienen:</p>

<ul>
  <li>Pipelines específicos.</li>
  <li>Mappings concretos.</li>
  <li>Políticas de lifecycle propias.</li>
</ul>

<p>El efecto era <strong>silencioso pero devastador</strong>:</p>

<ul>
  <li>Las transacciones seguían apareciendo.</li>
  <li>Las trazas seguían visibles.</li>
  <li>Pero los errores y stacktraces quedaban degradados o directamente invisibles.</li>
</ul>

<h2 id="por-qué-apm-parecía-vacío-al-filtrar">Por qué APM parecía “vacío” al filtrar</h2>

<p>La UI de APM depende críticamente de campos como:</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">service.name</code></li>
  <li><code class="language-plaintext highlighter-rouge">service.environment</code></li>
</ul>

<p>Cuando los templates de APM eran pisados:</p>

<ul>
  <li>Estos campos dejaban de comportarse como APM espera.</li>
  <li>No eran agregables de la forma correcta.</li>
  <li>Los filtros por servicio o entorno no devolvían resultados.</li>
</ul>

<p>Desde Kibana, la sensación era clara:</p>

<blockquote>
  <p>No hay errores.</p>
</blockquote>

<p>En realidad:</p>

<blockquote>
  <p>Los errores existían, pero APM no podía representarlos correctamente.</p>
</blockquote>

<h2 id="por-qué-este-problema-es-tan-difícil-de-detectar">Por qué este problema es tan difícil de detectar</h2>

<p>Elastic no lanza alertas por esto.
No hay errores evidentes.
No hay mensajes claros en la UI.</p>

<p>APM simplemente deja de mostrar parte de la información.</p>

<p>Y eso es lo más peligroso:</p>

<ul>
  <li>Crees que el problema es el agente.</li>
  <li>Ajustas instrumentación.</li>
  <li>Cambias sampling.</li>
  <li>Añades profiling.</li>
</ul>

<p>Cuando el problema real está en el ciclo de vida y los templates del dato.</p>

<h2 id="el-aprendizaje-real">El aprendizaje real</h2>

<p>Este caso dejó varias lecciones importantes:</p>

<ol>
  <li><strong>APM puede funcionar de forma parcial</strong> y aun así parecer “correcto”.</li>
  <li><strong>Ver transacciones no garantiza ver errores.</strong></li>
  <li><strong>Añadir más instrumentación</strong> puede mejorar el detalle, pero no arregla problemas de ingestión o lifecycle.</li>
  <li><strong>Los templates y políticas ILM de APM</strong> no son intercambiables con las de logs genéricos.</li>
</ol>

<p>Y la más importante:</p>

<blockquote>
  <p>En observabilidad, no basta con que los datos lleguen.
Tienen que llegar con la forma exacta que la herramienta espera.</p>
</blockquote>

<p><img src="/assets/img/elastic-image-3.png" alt="Plantillas de Elasticsearch y ILM" style="max-width: 600px; margin: 20px 0;" /></p>

<h2 id="cierre">Cierre</h2>

<p>Elastic APM estaba haciendo su trabajo desde el primer momento.
Los errores también existían.</p>

<p>El problema era menos visible y más peligroso:
una configuración genérica de logs que degradaba silenciosamente APM.</p>

<p>A veces el problema no es que no se generen, sino que se procesen de forma correcta.</p>

<p>Si estás operando Elastic y encuentras problemas similares en APM o observabilidad, puedes <a href="/es/contacto">contactarme</a>.</p>]]></content><author><name></name></author><summary type="html"><![CDATA[Elastic APM parecía funcionar perfectamente, pero los errores no aparecían. El problema no estaba en el agente, sino en los templates de logs.]]></summary><media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="http://localhost:4000/assets/img/elastic-image-title.png" /><media:content medium="image" url="http://localhost:4000/assets/img/elastic-image-title.png" xmlns:media="http://search.yahoo.com/mrss/" /></entry><entry xml:lang="en"><title type="html">When Elastic APM works… but you see no errors</title><link href="http://localhost:4000/2026/01/29/apm-works-but-no-errors-en/" rel="alternate" type="text/html" title="When Elastic APM works… but you see no errors" /><published>2026-01-29T00:00:00+01:00</published><updated>2026-01-29T00:00:00+01:00</updated><id>http://localhost:4000/2026/01/29/apm-works-but-no-errors-en</id><content type="html" xml:base="http://localhost:4000/2026/01/29/apm-works-but-no-errors-en/"><![CDATA[<p>Elastic APM was working.
Transactions were appearing.
Service calls were visible with reasonable clarity.</p>

<p>And yet, there was something deeply frustrating:
errors weren’t showing up.</p>

<p>The application had real exceptions, visible in the logs, but in the APM UI:</p>

<ul>
  <li>No errors.</li>
  <li>No stacktraces.</li>
  <li>Traces looked “clean”.</li>
</ul>

<p>The problem wasn’t obvious. And it wasn’t where I thought it was.</p>

<h2 id="the-context">The context</h2>

<p>I was integrating Elastic APM into a Java application running on Tomcat.</p>

<p>The basic integration was correct:</p>

<ul>
  <li>The APM agent started without errors.</li>
  <li>APM showed services and transactions.</li>
  <li>Requests were traced correctly.</li>
</ul>

<p>On the surface, APM worked.
For diagnostics, it didn’t.</p>

<h2 id="the-real-symptom-traces-without-error-context">The real symptom: traces without error context</h2>

<p>Traces showed:</p>

<ul>
  <li>Durations.</li>
  <li>Calls.</li>
  <li>The overall flow of the request.</li>
</ul>

<p>But it was missing the most important thing when something failed:</p>

<ul>
  <li>No associated error.</li>
  <li>No stacktrace.</li>
  <li>No clear hint of the real reason for the failure.</li>
</ul>

<p>This is especially deceptive because APM wasn’t empty.
It was incomplete.</p>

<p><img src="/assets/img/elastic-image-1.png" alt="Elastic APM without errors" style="max-width: 600px; margin: 20px 0;" /></p>

<h2 id="first-instinct-add-more-instrumentation">First instinct: add more instrumentation</h2>

<p>My first reaction was to think the problem was insufficient detail in the traces.</p>

<p>I initially tried to force instrumentation with:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ELASTIC_APM_TRACE_METHODS=com.pierinho13.*#*
</code></pre></div></div>

<p>The result was immediate, but not useful long-term:</p>

<ul>
  <li>Much more detail.</li>
  <li>Too many methods instrumented.</li>
  <li>A volume of spans hard to manage.</li>
  <li>Excessive noise for a real environment.</li>
</ul>

<p>APM stopped being a diagnostic tool and became a data volcano.</p>

<h2 id="a-better-balance-inferred-spans-via-profiling">A better balance: inferred spans via profiling</h2>

<p>Looking for a more reasonable alternative, I enabled inferred spans through profiling:</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">ELASTIC_APM_PROFILING_INFERRED_SPANS_ENABLED</span>
  <span class="na">value</span><span class="pi">:</span> <span class="s2">"</span><span class="s">true"</span>

<span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">ELASTIC_APM_PROFILING_INFERRED_SPANS_MIN_DURATION</span>
  <span class="na">value</span><span class="pi">:</span> <span class="s2">"</span><span class="s">10ms"</span>

<span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">ELASTIC_APM_PROFILING_INFERRED_SPANS_SAMPLING_INTERVAL</span>
  <span class="na">value</span><span class="pi">:</span> <span class="s2">"</span><span class="s">50ms"</span>
</code></pre></div></div>

<p>There was a clear shift here:</p>

<ul>
  <li>Traces started showing much more internal detail.</li>
  <li>The actual path of the request was easier to understand.</li>
  <li>Without the cost or verbosity of TRACE_METHODS.</li>
</ul>

<p>APM started being truly useful.</p>

<p>But errors still didn’t appear.</p>

<p><img src="/assets/img/elastic-image-2.png" alt="Inferred spans with profiling" style="max-width: 600px; margin: 20px 0;" /></p>

<h2 id="the-focus-mistake">The focus mistake</h2>

<p>At that point it was clear that:</p>

<ul>
  <li>The agent worked.</li>
  <li>Instrumentation worked.</li>
  <li>Traces were being generated correctly.</li>
</ul>

<p>The problem wasn’t in how data was captured, but in what happened to it after.</p>

<h2 id="the-real-culprit-templates-and-ilm-overriding-apm">The real culprit: templates and ILM overriding APM</h2>

<p>The real cause wasn’t in APM, but in log management.</p>

<p>There was a <strong>generic template for <code class="language-plaintext highlighter-rouge">logs-*-*</code></strong> with higher priority than APM-specific templates.
This meant that for data streams like <code class="language-plaintext highlighter-rouge">logs-apm.error-*</code>, the generic template would win instead of what Fleet and APM expect.</p>

<p>Also, ILM operations were being applied indiscriminately to all data streams under <code class="language-plaintext highlighter-rouge">logs-*</code>, including APM’s, which have:</p>

<ul>
  <li>Specific pipelines.</li>
  <li>Concrete mappings.</li>
  <li>Their own lifecycle policies.</li>
</ul>

<p>The effect was <strong>silent but devastating</strong>:</p>

<ul>
  <li>Transactions kept appearing.</li>
  <li>Traces remained visible.</li>
  <li>But errors and stacktraces were degraded or directly invisible.</li>
</ul>

<h2 id="why-apm-seemed-empty-when-filtering">Why APM seemed “empty” when filtering</h2>

<p>The APM UI depends critically on fields like:</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">service.name</code></li>
  <li><code class="language-plaintext highlighter-rouge">service.environment</code></li>
</ul>

<p>When APM templates were being overridden:</p>

<ul>
  <li>These fields stopped behaving as APM expects.</li>
  <li>They weren’t aggregatable the right way.</li>
  <li>Filters by service or environment returned nothing.</li>
</ul>

<p>From Kibana, the feeling was clear:</p>

<blockquote>
  <p>No errors.</p>
</blockquote>

<p>In reality:</p>

<blockquote>
  <p>Errors existed, but APM couldn’t represent them correctly.</p>
</blockquote>

<h2 id="why-this-problem-is-so-hard-to-detect">Why this problem is so hard to detect</h2>

<p>Elastic doesn’t raise alerts for this.
There are no obvious errors.
No clear messages in the UI.</p>

<p>APM simply stops showing part of the information.</p>

<p>And that’s the most dangerous part:</p>

<ul>
  <li>You think the problem is the agent.</li>
  <li>You adjust instrumentation.</li>
  <li>You change sampling.</li>
  <li>You add profiling.</li>
</ul>

<p>When the real problem is in the data’s lifecycle and templates.</p>

<h2 id="the-real-lesson">The real lesson</h2>

<p>This case left several important lessons:</p>

<ol>
  <li><strong>APM can work partially</strong> and still seem “correct”.</li>
  <li><strong>Seeing transactions doesn’t guarantee seeing errors.</strong></li>
  <li><strong>Adding more instrumentation</strong> can improve detail, but doesn’t fix ingestion or lifecycle problems.</li>
  <li><strong>APM’s templates and ILM policies</strong> aren’t interchangeable with generic log ones.</li>
</ol>

<p>And the most important:</p>

<blockquote>
  <p>In observability, it’s not enough for data to arrive.
It has to arrive in the exact shape the tool expects.</p>
</blockquote>

<p><img src="/assets/img/elastic-image-3.png" alt="Elasticsearch templates and ILM" style="max-width: 600px; margin: 20px 0;" /></p>

<h2 id="closing">Closing</h2>

<p>Elastic APM was doing its job from the first moment.
The errors existed too.</p>

<p>The problem was less visible and more dangerous:
generic log configuration silently degrading APM.</p>

<p>Sometimes the problem isn’t that data isn’t generated, but that it’s processed correctly.</p>

<p>If you’re operating Elastic and encounter similar APM or observability issues, you can <a href="/en/contact">contact me</a>.</p>]]></content><author><name></name></author><summary type="html"><![CDATA[Elastic APM seemed to work perfectly, but errors weren't showing up. The problem wasn't with the agent—it was with log templates.]]></summary><media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="http://localhost:4000/assets/img/elastic-image-title.png" /><media:content medium="image" url="http://localhost:4000/assets/img/elastic-image-title.png" xmlns:media="http://search.yahoo.com/mrss/" /></entry><entry xml:lang="es"><title type="html">¿Cómo estructurar tus proyectos Terraform? Workspaces vs repositorios por entorno</title><link href="http://localhost:4000/2026/01/21/estructuras-terraform-workspaces-modulos-versionados-es/" rel="alternate" type="text/html" title="¿Cómo estructurar tus proyectos Terraform? Workspaces vs repositorios por entorno" /><published>2026-01-21T00:00:00+01:00</published><updated>2026-01-21T00:00:00+01:00</updated><id>http://localhost:4000/2026/01/21/estructuras-terraform-workspaces-modulos-versionados-es</id><content type="html" xml:base="http://localhost:4000/2026/01/21/estructuras-terraform-workspaces-modulos-versionados-es/"><![CDATA[<p>En muchos equipos es habitual encontrar una estructura de Terraform basada en un único repositorio, con carpetas por entorno (prod, preprod, staging) y una carpeta común de módulos reutilizables. Aunque no es un estándar oficial definido por Terraform, sí es un patrón muy extendido en la comunidad, especialmente en equipos que empiezan a escalar su infraestructura como código.</p>

<p>Un ejemplo típico de este enfoque sería el siguiente:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>terraform-infra/
├── modules/
│   ├── vpc/
│   │   ├── main.tf
│   │   ├── variables.tf
│   │   └── outputs.tf
│   ├── eks/
│   │   ├── main.tf
│   │   ├── variables.tf
│   │   └── outputs.tf
│   └── rds/
│       ├── main.tf
│       ├── variables.tf
│       └── outputs.tf
├── prod/
│   ├── backend.tf
│   ├── providers.tf
│   ├── main.tf
│   ├── variables.tf
│   └── terraform.tfvars
├── preprod/
│   ├── backend.tf
│   ├── providers.tf
│   ├── main.tf
│   ├── variables.tf
│   └── terraform.tfvars
└── staging/
    ├── backend.tf
    ├── providers.tf
    ├── main.tf
    ├── variables.tf
    └── terraform.tfvars
</code></pre></div></div>

<p>En este modelo, cada entorno referencia los módulos locales definidos dentro del mismo repositorio, por ejemplo desde <code class="language-plaintext highlighter-rouge">prod/main.tf</code>:</p>

<div class="language-hcl highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nx">module</span> <span class="s2">"vpc"</span> <span class="p">{</span>
  <span class="nx">source</span> <span class="o">=</span> <span class="s2">"../modules/vpc"</span>
  <span class="nx">name</span>   <span class="o">=</span> <span class="s2">"prod"</span>
<span class="p">}</span>

<span class="nx">module</span> <span class="s2">"eks"</span> <span class="p">{</span>
  <span class="nx">source</span> <span class="o">=</span> <span class="s2">"../modules/eks"</span>
<span class="p">}</span>
</code></pre></div></div>

<p>Este enfoque funciona bien en equipos pequeños o en fases iniciales, y suele combinarse con <strong>Terraform workspaces</strong> para gestionar múltiples estados desde la misma base de código.</p>

<h2 id="workspaces-contexto-y-limitaciones">Workspaces: contexto y limitaciones</h2>

<p>Terraform, por diseño, es flexible respecto a la organización del código y no impone una estructura concreta. En este contexto, los Terraform workspaces suelen aparecer como una solución natural para gestionar múltiples entornos desde la misma base de código, algo que encaja especialmente bien con <strong>Terraform Cloud</strong>, donde el workspace se selecciona explícitamente desde la interfaz y queda claramente asociado a un estado, variables y políticas concretas.</p>

<p>Sin embargo, fuera de Terraform Cloud, el uso de workspaces introduce ciertos riesgos operativos. En ejecución local o en pipelines genéricas, el workspace activo depende del contexto del comando (<code class="language-plaintext highlighter-rouge">terraform workspace select</code>). Esto hace que el error humano sea más probable: un <code class="language-plaintext highlighter-rouge">apply</code> ejecutado en el workspace incorrecto puede impactar en el entorno equivocado si no existen controles adicionales.</p>

<p>La propia documentación oficial de Terraform reconoce esta limitación y, en la sección <strong>Alternatives to Workspaces</strong>, propone una estrategia distinta para escenarios más complejos:</p>

<blockquote>
  <p>“Instead of creating CLI workspaces, you can use one or more re-usable modules to represent the common elements and then represent each instance as a separate configuration that instantiates those common elements in the context of a different backend.”</p>

  <p>— <a href="https://developer.hashicorp.com/terraform/cli/workspaces#alternatives-to-workspaces">Terraform CLI – Workspaces</a></p>
</blockquote>

<p>Esto respalda la idea de que, cuando los entornos requieren un aislamiento claro —por ejemplo distintos backends, credenciales o dominios de responsabilidad— usar configuraciones separadas que consumen módulos reutilizables puede ser más robusto que depender únicamente de workspaces en la CLI.</p>

<h2 id="un-repositorio-por-entorno">Un repositorio por entorno</h2>

<p>Desde esta perspectiva, una alternativa habitual en entornos productivos es utilizar <strong>un repositorio por entorno</strong>. En este modelo, cada entorno (producción, preproducción, etc.) tiene su propio repositorio, su propio backend, su propio estado y su propio control de accesos, reduciendo significativamente el riesgo de errores operativos.</p>

<p>La reutilización se consigue mediante <strong>módulos compartidos</strong>, idealmente alojados en repositorios independientes, con versionado explícito y controlado. Terraform no solo soporta este enfoque, sino que lo documenta de forma clara.</p>

<p>En la documentación oficial sobre configuración de módulos, Terraform explica cómo instalar versiones concretas de un módulo, tanto desde registries como desde repositorios Git, permitiendo fijar una versión mediante tags, ramas o hashes:</p>

<blockquote>
  <p>“If you are using modules hosted in GitHub, BitBucket, or another Git repository, Terraform clones and uses the default branch referenced by HEAD. You can add the ref query parameter to the location specified in the source argument to reference any value supported by the git checkout command, such as a branch, SHA-1 hash, or tag.”</p>

  <p>— <a href="https://developer.hashicorp.com/terraform/language/modules/configuration">Terraform – Module Configuration</a></p>
</blockquote>

<p>La propia documentación incluye ejemplos explícitos utilizando tags versionados y hashes para fijar la versión exacta del módulo:</p>

<div class="language-hcl highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nx">module</span> <span class="s2">"vpc"</span> <span class="p">{</span>
  <span class="nx">source</span> <span class="o">=</span> <span class="s2">"git::https://example.com/vpc.git?ref=v1.2.0"</span>
<span class="p">}</span>

<span class="nx">module</span> <span class="s2">"storage"</span> <span class="p">{</span>
  <span class="nx">source</span> <span class="o">=</span> <span class="s2">"git::https://example.com/storage.git?ref=51d462976d84fdea54b47d80dcabbf680badcdb8"</span>
<span class="p">}</span>
</code></pre></div></div>

<p>Este enfoque permite que distintos entornos consuman el mismo módulo, pero con versiones distintas y variables distintas, facilitando una evolución controlada de la infraestructura sin romper entornos existentes.</p>

<h2 id="ejemplo-práctico-repositorios-por-entorno-con-módulos-versionados">Ejemplo práctico: repositorios por entorno con módulos versionados</h2>

<p>Una posible materialización práctica de este enfoque es separar los entornos en repositorios independientes y consumir módulos compartidos desde repositorios externos, fijando versiones concretas.</p>

<h3 id="repositorios-de-módulos-compartidos">Repositorios de módulos (compartidos)</h3>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>tf-modules-vpc/        (tags: v1.2.0, v1.3.0, v2.0.0)
tf-modules-eks/        (tags: v0.9.1, v1.0.0)
tf-modules-rds/        (tags: v3.4.2)
</code></pre></div></div>

<p>Cada módulo se desarrolla, evoluciona y versiona de forma independiente.</p>

<h3 id="repositorio-de-entorno-preproducción">Repositorio de entorno: preproducción</h3>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>infra-preprod/
├── backend.tf
├── providers.tf
├── versions.tf
├── main.tf
├── variables.tf
└── env/
    └── preprod.tfvars
</code></pre></div></div>

<p>Ejemplo de consumo de módulos en <code class="language-plaintext highlighter-rouge">infra-preprod/main.tf</code>:</p>

<div class="language-hcl highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nx">module</span> <span class="s2">"vpc"</span> <span class="p">{</span>
  <span class="nx">source</span> <span class="o">=</span> <span class="s2">"git::https://github.com/tu-org/tf-modules-vpc.git?ref=v1.3.0"</span>
  <span class="nx">name</span>   <span class="o">=</span> <span class="s2">"preprod"</span>
<span class="p">}</span>

<span class="nx">module</span> <span class="s2">"eks"</span> <span class="p">{</span>
  <span class="nx">source</span> <span class="o">=</span> <span class="s2">"git::https://github.com/tu-org/tf-modules-eks.git?ref=v1.0.0"</span>
<span class="p">}</span>
</code></pre></div></div>

<h3 id="repositorio-de-entorno-producción">Repositorio de entorno: producción</h3>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>infra-prod/
├── backend.tf
├── providers.tf
├── versions.tf
├── main.tf
├── variables.tf
└── env/
    └── prod.tfvars
</code></pre></div></div>

<p>Ejemplo de consumo de módulos en <code class="language-plaintext highlighter-rouge">infra-prod/main.tf</code>:</p>

<div class="language-hcl highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nx">module</span> <span class="s2">"vpc"</span> <span class="p">{</span>
  <span class="nx">source</span> <span class="o">=</span> <span class="s2">"git::https://github.com/tu-org/tf-modules-vpc.git?ref=v1.2.0"</span>
  <span class="nx">name</span>   <span class="o">=</span> <span class="s2">"prod"</span>
<span class="p">}</span>

<span class="nx">module</span> <span class="s2">"eks"</span> <span class="p">{</span>
  <span class="nx">source</span> <span class="o">=</span> <span class="s2">"git::https://github.com/tu-org/tf-modules-eks.git?ref=v0.9.1"</span>
<span class="p">}</span>
</code></pre></div></div>

<h3 id="ventajas-de-este-modelo">Ventajas de este modelo</h3>

<p>De este modo:</p>

<ul>
  <li>Ambos entornos reutilizan los mismos módulos</li>
  <li>Cada entorno fija explícitamente la versión del módulo que consume</li>
  <li>Preproducción puede avanzar versiones antes que producción</li>
  <li>Producción permanece estable hasta que se decide actualizar</li>
</ul>

<h2 id="conclusión">Conclusión</h2>

<p>Los Terraform workspaces encajan muy bien cuando se usan junto con Terraform Cloud y controles claros desde la plataforma. Sin embargo, para organizaciones con múltiples entornos críticos, pipelines automatizadas y necesidades fuertes de aislamiento, un modelo basado en <strong>repositorios por entorno</strong>, combinado con <strong>módulos reutilizables versionados</strong>, suele ofrecer mayor claridad, control y seguridad operativa a largo plazo.</p>

<h2 id="otros-recursos-interesantes-de-hashicorp">Otros recursos interesantes de HashiCorp</h2>

<p>Para profundizar en los distintos enfoques de organización de código Terraform y entender mejor los trade-offs entre estructuras, repositorios y módulos, estos recursos oficiales de HashiCorp resultan especialmente útiles:</p>

<p><strong><a href="https://www.hashicorp.com/en/blog/terraform-mono-repo-vs-multi-repo-the-great-debate">Terraform Mono-Repo vs Multi-Repo: The Great Debate</a></strong><br />
Análisis de ventajas y desventajas entre enfoques monorepo y multi-repo aplicados a Terraform, destacando que la decisión depende del contexto organizativo y operativo.</p>

<p><strong><a href="https://www.hashicorp.com/en/blog/structuring-hashicorp-terraform-configuration-for-production">Structuring Terraform Configuration for Production</a></strong><br />
Recomendaciones para evolucionar configuraciones Terraform hacia entornos productivos, enfatizando la separación de responsabilidades y el uso de módulos reutilizables.</p>

<p><strong><a href="https://developer.hashicorp.com/terraform/cli/workspaces#alternatives-to-workspaces">Terraform CLI – Workspaces (Alternatives to Workspaces)</a></strong><br />
Documentación oficial donde HashiCorp describe explícitamente alternativas a los workspaces basadas en configuraciones separadas que reutilizan módulos comunes con backends distintos.</p>

<p><strong><a href="https://developer.hashicorp.com/terraform/language/modules/configuration">Terraform – Module Configuration</a></strong><br />
Documentación oficial sobre cómo consumir módulos desde registries o repositorios Git, incluyendo versionado mediante tags, ramas o hashes.</p>

<p>Si te interesa este tipo de arquitecturas, puedes ver más sobre mi experiencia o <a href="/es/contacto">contactarme</a>.</p>]]></content><author><name></name></author><summary type="html"><![CDATA[En muchos equipos es habitual encontrar una estructura de Terraform basada en un único repositorio con carpetas por entorno. Aunque funciona, existen alternativas más robustas para entornos productivos.]]></summary><media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="http://localhost:4000/assets/img/non_recommended_structure_terraform.png" /><media:content medium="image" url="http://localhost:4000/assets/img/non_recommended_structure_terraform.png" xmlns:media="http://search.yahoo.com/mrss/" /></entry><entry xml:lang="en"><title type="html">How to structure your Terraform projects? Workspaces vs per-environment repositories</title><link href="http://localhost:4000/2026/01/21/terraform-structures-workspaces-versioned-modules-en/" rel="alternate" type="text/html" title="How to structure your Terraform projects? Workspaces vs per-environment repositories" /><published>2026-01-21T00:00:00+01:00</published><updated>2026-01-21T00:00:00+01:00</updated><id>http://localhost:4000/2026/01/21/terraform-structures-workspaces-versioned-modules-en</id><content type="html" xml:base="http://localhost:4000/2026/01/21/terraform-structures-workspaces-versioned-modules-en/"><![CDATA[<p>Many teams commonly use a Terraform structure based on a single repository, with folders per environment (prod, preprod, staging) and a common folder for reusable modules. Although this is not an official standard defined by Terraform, it is a widely adopted pattern in the community, especially in teams that are starting to scale their infrastructure as code.</p>

<p>A typical example of this approach would be:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>terraform-infra/
├── modules/
│   ├── vpc/
│   │   ├── main.tf
│   │   ├── variables.tf
│   │   └── outputs.tf
│   ├── eks/
│   │   ├── main.tf
│   │   ├── variables.tf
│   │   └── outputs.tf
│   └── rds/
│       ├── main.tf
│       ├── variables.tf
│       └── outputs.tf
├── prod/
│   ├── backend.tf
│   ├── providers.tf
│   ├── main.tf
│   ├── variables.tf
│   └── terraform.tfvars
├── preprod/
│   ├── backend.tf
│   ├── providers.tf
│   ├── main.tf
│   ├── variables.tf
│   └── terraform.tfvars
└── staging/
    ├── backend.tf
    ├── providers.tf
    ├── main.tf
    ├── variables.tf
    └── terraform.tfvars
</code></pre></div></div>

<p>In this model, each environment references the local modules defined within the same repository, for example from <code class="language-plaintext highlighter-rouge">prod/main.tf</code>:</p>

<div class="language-hcl highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nx">module</span> <span class="s2">"vpc"</span> <span class="p">{</span>
  <span class="nx">source</span> <span class="o">=</span> <span class="s2">"../modules/vpc"</span>
  <span class="nx">name</span>   <span class="o">=</span> <span class="s2">"prod"</span>
<span class="p">}</span>

<span class="nx">module</span> <span class="s2">"eks"</span> <span class="p">{</span>
  <span class="nx">source</span> <span class="o">=</span> <span class="s2">"../modules/eks"</span>
<span class="p">}</span>
</code></pre></div></div>

<p>This approach works well in small teams or early stages, and is often combined with <strong>Terraform workspaces</strong> to manage multiple states from the same codebase.</p>

<h2 id="workspaces-context-and-limitations">Workspaces: context and limitations</h2>

<p>Terraform, by design, is flexible regarding code organization and does not impose a specific structure. In this context, Terraform workspaces often appear as a natural solution for managing multiple environments from the same codebase, something that fits especially well with <strong>Terraform Cloud</strong>, where the workspace is explicitly selected from the interface and is clearly associated with a specific state, variables, and policies.</p>

<p>However, outside of Terraform Cloud, using workspaces introduces certain operational risks. In local execution or generic pipelines, the active workspace depends on the command context (<code class="language-plaintext highlighter-rouge">terraform workspace select</code>). This makes human error more likely: an <code class="language-plaintext highlighter-rouge">apply</code> executed in the wrong workspace can impact the wrong environment if there are no additional controls.</p>

<p>Terraform’s official documentation acknowledges this limitation and, in the <strong>Alternatives to Workspaces</strong> section, proposes a different strategy for more complex scenarios:</p>

<blockquote>
  <p>“Instead of creating CLI workspaces, you can use one or more re-usable modules to represent the common elements and then represent each instance as a separate configuration that instantiates those common elements in the context of a different backend.”</p>

  <p>— <a href="https://developer.hashicorp.com/terraform/cli/workspaces#alternatives-to-workspaces">Terraform CLI – Workspaces</a></p>
</blockquote>

<p>This supports the idea that, when environments require clear isolation — for example, different backends, credentials, or domains of responsibility — using separate configurations that consume reusable modules can be more robust than relying solely on CLI workspaces.</p>

<h2 id="one-repository-per-environment">One repository per environment</h2>

<p>From this perspective, a common alternative in production environments is to use <strong>one repository per environment</strong>. In this model, each environment (production, preproduction, etc.) has its own repository, its own backend, its own state, and its own access controls, significantly reducing the risk of operational errors.</p>

<p>Reusability is achieved through <strong>shared modules</strong>, ideally hosted in independent repositories, with explicit and controlled versioning. Terraform not only supports this approach but documents it clearly.</p>

<p>In the official documentation on module configuration, Terraform explains how to install specific versions of a module, both from registries and Git repositories, allowing you to pin a version using tags, branches, or hashes:</p>

<blockquote>
  <p>“If you are using modules hosted in GitHub, BitBucket, or another Git repository, Terraform clones and uses the default branch referenced by HEAD. You can add the ref query parameter to the location specified in the source argument to reference any value supported by the git checkout command, such as a branch, SHA-1 hash, or tag.”</p>

  <p>— <a href="https://developer.hashicorp.com/terraform/language/modules/configuration">Terraform – Module Configuration</a></p>
</blockquote>

<p>The documentation includes explicit examples using versioned tags and hashes to pin the exact module version:</p>

<div class="language-hcl highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nx">module</span> <span class="s2">"vpc"</span> <span class="p">{</span>
  <span class="nx">source</span> <span class="o">=</span> <span class="s2">"git::https://example.com/vpc.git?ref=v1.2.0"</span>
<span class="p">}</span>

<span class="nx">module</span> <span class="s2">"storage"</span> <span class="p">{</span>
  <span class="nx">source</span> <span class="o">=</span> <span class="s2">"git::https://example.com/storage.git?ref=51d462976d84fdea54b47d80dcabbf680badcdb8"</span>
<span class="p">}</span>
</code></pre></div></div>

<p>This approach allows different environments to consume the same module, but with different versions and variables, enabling controlled infrastructure evolution without breaking existing environments.</p>

<h2 id="practical-example-repositories-per-environment-with-versioned-modules">Practical example: repositories per environment with versioned modules</h2>

<p>A practical implementation of this approach is to separate environments into independent repositories and consume shared modules from external repositories, pinning specific versions.</p>

<h3 id="module-repositories-shared">Module repositories (shared)</h3>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>tf-modules-vpc/        (tags: v1.2.0, v1.3.0, v2.0.0)
tf-modules-eks/        (tags: v0.9.1, v1.0.0)
tf-modules-rds/        (tags: v3.4.2)
</code></pre></div></div>

<p>Each module is developed, evolved, and versioned independently.</p>

<h3 id="environment-repository-preproduction">Environment repository: preproduction</h3>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>infra-preprod/
├── backend.tf
├── providers.tf
├── versions.tf
├── main.tf
├── variables.tf
└── env/
    └── preprod.tfvars
</code></pre></div></div>

<p>Example of module consumption in <code class="language-plaintext highlighter-rouge">infra-preprod/main.tf</code>:</p>

<div class="language-hcl highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nx">module</span> <span class="s2">"vpc"</span> <span class="p">{</span>
  <span class="nx">source</span> <span class="o">=</span> <span class="s2">"git::https://github.com/your-org/tf-modules-vpc.git?ref=v1.3.0"</span>
  <span class="nx">name</span>   <span class="o">=</span> <span class="s2">"preprod"</span>
<span class="p">}</span>

<span class="nx">module</span> <span class="s2">"eks"</span> <span class="p">{</span>
  <span class="nx">source</span> <span class="o">=</span> <span class="s2">"git::https://github.com/your-org/tf-modules-eks.git?ref=v1.0.0"</span>
<span class="p">}</span>
</code></pre></div></div>

<h3 id="environment-repository-production">Environment repository: production</h3>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>infra-prod/
├── backend.tf
├── providers.tf
├── versions.tf
├── main.tf
├── variables.tf
└── env/
    └── prod.tfvars
</code></pre></div></div>

<p>Example of module consumption in <code class="language-plaintext highlighter-rouge">infra-prod/main.tf</code>:</p>

<div class="language-hcl highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nx">module</span> <span class="s2">"vpc"</span> <span class="p">{</span>
  <span class="nx">source</span> <span class="o">=</span> <span class="s2">"git::https://github.com/your-org/tf-modules-vpc.git?ref=v1.2.0"</span>
  <span class="nx">name</span>   <span class="o">=</span> <span class="s2">"prod"</span>
<span class="p">}</span>

<span class="nx">module</span> <span class="s2">"eks"</span> <span class="p">{</span>
  <span class="nx">source</span> <span class="o">=</span> <span class="s2">"git::https://github.com/your-org/tf-modules-eks.git?ref=v0.9.1"</span>
<span class="p">}</span>
</code></pre></div></div>

<h3 id="advantages-of-this-model">Advantages of this model</h3>

<p>With this approach:</p>

<ul>
  <li>Both environments reuse the same modules</li>
  <li>Each environment explicitly pins the module version it consumes</li>
  <li>Preproduction can advance versions before production</li>
  <li>Production remains stable until an update is decided</li>
</ul>

<h2 id="conclusion">Conclusion</h2>

<p>Terraform workspaces fit very well when used alongside Terraform Cloud and clear controls from the platform. However, for organizations with multiple critical environments, automated pipelines, and strong isolation needs, a model based on <strong>repositories per environment</strong>, combined with <strong>versioned reusable modules</strong>, typically offers greater clarity, control, and long-term operational security.</p>

<h2 id="other-useful-hashicorp-resources">Other useful HashiCorp resources</h2>

<p>To dive deeper into different approaches for organizing Terraform code and better understand the trade-offs between structures, repositories, and modules, these official HashiCorp resources are especially useful:</p>

<p><strong><a href="https://www.hashicorp.com/en/blog/terraform-mono-repo-vs-multi-repo-the-great-debate">Terraform Mono-Repo vs Multi-Repo: The Great Debate</a></strong><br />
An analysis of the advantages and disadvantages between monorepo and multi-repo approaches applied to Terraform, highlighting that the decision depends on organizational and operational context.</p>

<p><strong><a href="https://www.hashicorp.com/en/blog/structuring-hashicorp-terraform-configuration-for-production">Structuring Terraform Configuration for Production</a></strong><br />
Recommendations for evolving Terraform configurations toward production environments, emphasizing separation of responsibilities and the use of reusable modules.</p>

<p><strong><a href="https://developer.hashicorp.com/terraform/cli/workspaces#alternatives-to-workspaces">Terraform CLI – Workspaces (Alternatives to Workspaces)</a></strong><br />
Official documentation where HashiCorp explicitly describes alternatives to workspaces based on separate configurations that reuse common modules with different backends.</p>

<p><strong><a href="https://developer.hashicorp.com/terraform/language/modules/configuration">Terraform – Module Configuration</a></strong><br />
Official documentation on how to consume modules from registries or Git repositories, including versioning via tags, branches, or hashes.</p>

<p>If you’re interested in this kind of architecture, you can learn more about my experience or <a href="/en/contact">get in touch</a>.</p>]]></content><author><name></name></author><summary type="html"><![CDATA[Many teams use a Terraform structure based on a single repository with folders per environment. While it works, there are more robust alternatives for production environments.]]></summary><media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="http://localhost:4000/assets/img/non_recommended_structure_terraform.png" /><media:content medium="image" url="http://localhost:4000/assets/img/non_recommended_structure_terraform.png" xmlns:media="http://search.yahoo.com/mrss/" /></entry></feed>